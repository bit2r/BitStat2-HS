---
title: "확률변수와 기댓값"
---

::: {.hs-back}
[← 빛스탯 HS 홈](../../../index.qmd)
:::

::: {.hs-meta}
단원 03 확률분포 · 「확률과 통계」 · 1차시(45분)
:::

## 개념

어떤 시행의 결과에 따라 값이 정해지는 변수를 **확률변수** $X$라 하고,
$X$가 가질 수 있는 각 값에 확률을 대응시킨 것을 **확률분포**라고 합니다.
확률변수 $X$의 **기댓값**(평균)은 각 값에 그 확률을 곱해 모두 더한 값입니다.

$$E(X) = x_1 p_1 + x_2 p_2 + \cdots + x_n p_n$$

::: {.column-margin}
복권 한 장의 "평균 당첨금"이 바로 기댓값입니다. 아래 앱에서 복권 시나리오를
골라 기댓값과 복권 가격을 비교해 보세요.
:::

시행을 많이 반복하면 실제 관측값들의 평균(표본평균)은 기댓값에 가까워집니다.
아래 앱에서 시행 횟수를 늘려가며 두 값이 가까워지는 모습을 직접 확인해 보세요.

## 만지며 배우기

::: {.panel-tabset}

### 인터랙티브 앱

```{shinylive-python}
#| standalone: true
#| viewerHeight: 640

from shiny import App, reactive, render, ui
import numpy as np
import matplotlib
import matplotlib.pyplot as plt

matplotlib.rcParams["font.size"] = 10

ACCENT = "#2563eb"
INK = "#111827"
MUTED = "#6b7280"

SCENARIOS = {
    "die": {"name": "주사위 한 개의 눈",
            "x": [1, 2, 3, 4, 5, 6],
            "p": [1/6] * 6},
    "coins": {"name": "동전 두 개의 앞면 수",
              "x": [0, 1, 2],
              "p": [0.25, 0.5, 0.25]},
    "lotto": {"name": "복권 한 장의 당첨금(원)",
              "x": [0, 1000, 10000, 1000000],
              "p": [0.899, 0.09, 0.01, 0.001]},
}

def tufte(ax):
    for side in ("top", "right"):
        ax.spines[side].set_visible(False)
    for side in ("left", "bottom"):
        ax.spines[side].set_color("#9ca3af")
    ax.tick_params(colors=MUTED, labelsize=9)

app_ui = ui.page_sidebar(
    ui.sidebar(
        ui.input_select(
            "scenario", "확률변수 X",
            {k: v["name"] for k, v in SCENARIOS.items()},
        ),
        ui.input_slider("n", "시행 횟수 n", min=10, max=5000, value=100, step=10),
        ui.input_action_button("redraw", "다시 추첨"),
        width=240,
    ),
    ui.card(
        ui.card_header("확률분포(테두리) vs 실제 시행의 상대도수(파랑)"),
        ui.output_plot("dist_plot", height="340px"),
    ),
    ui.card(ui.output_ui("readout")),
    fillable=True,
)

def server(input, output, session):
    @reactive.calc
    def draws():
        input.redraw()  # 버튼을 누르면 새로 추첨
        sc = SCENARIOS[input.scenario()]
        idx = np.random.choice(len(sc["x"]), size=input.n(), p=sc["p"])
        return np.array(sc["x"])[idx]

    @render.plot(alt="확률분포와 상대도수 비교")
    def dist_plot():
        sc = SCENARIOS[input.scenario()]
        vals = draws()
        pos = np.arange(len(sc["x"]))
        freq = [np.mean(vals == v) for v in sc["x"]]

        fig, ax = plt.subplots()
        ax.bar(pos - 0.2, sc["p"], width=0.38, fill=False,
               edgecolor=INK, linewidth=1.2, label="P(X=x)")
        ax.bar(pos + 0.2, freq, width=0.38, color=ACCENT, alpha=0.85,
               label="relative freq.")
        ax.set_xticks(pos)
        ax.set_xticklabels([f"{v:,}" for v in sc["x"]])
        ax.set_ylabel("P(X = x)", color=MUTED)
        ax.legend(frameon=False, fontsize=9)
        tufte(ax)
        return fig

    @render.ui
    def readout():
        sc = SCENARIOS[input.scenario()]
        e = sum(x * p for x, p in zip(sc["x"], sc["p"]))
        m = draws().mean()
        line = (f"기댓값 E(X) = {e:,.1f} · "
                f"시행 {input.n():,}번의 표본평균 = {m:,.1f} · "
                f"차이 = {abs(e - m):,.1f}")
        extra = ""
        if input.scenario() == "lotto":
            extra = (" — 복권 한 장의 기댓값은 약 1,190원입니다. "
                     "가격이 2,000원이라면 살수록 평균적으로 손해입니다.")
        return ui.HTML(f'<p class="hs-readline">{line}{extra}</p>')

app = App(app_ui, server)
```

### 파이썬 코드

앱의 핵심 계산은 이 몇 줄이 전부입니다.

```python
import numpy as np

x = [0, 1000, 10000, 1000000]          # 복권 당첨금
p = [0.899, 0.09, 0.01, 0.001]         # 각 확률

E = sum(xi * pi for xi, pi in zip(x, p))   # 기댓값 E(X) = 1190.0

draws = np.random.choice(x, size=5000, p=p)  # 5000번 시행
print(E, draws.mean())                       # 기댓값 vs 표본평균
```

:::

## 스스로 확인

::: {.callout-tip collapse="true" title="문제 1 — 시행 횟수를 10에서 5000까지 늘리면 표본평균은 어떻게 변하는가?"}
표본평균이 기댓값 $E(X)$에 점점 가까워집니다. 시행 횟수가 적을 때는
추첨할 때마다 표본평균이 크게 출렁이지만, 횟수가 커지면 안정됩니다.
(대수의 법칙 — 04단원 표본평균의 분포에서 다시 만납니다.)
:::

::: {.callout-tip collapse="true" title="문제 2 — 복권 시나리오에서 '다시 추첨'을 여러 번 눌러 보라. n=100일 때 표본평균이 유독 크게 튀는 이유는?"}
당첨금 1,000,000원의 확률이 0.001이라 100번 추첨에는 대개 한 번도
나오지 않다가, 어쩌다 한 번 나오면 표본평균이 크게 뜁니다.
드문 값이 평균에 미치는 영향을 눈으로 확인해 보세요.
:::

::: {.hs-prevnext}
[← 이전: 조건부확률과 독립](../../02_prob/conditional/index.qmd)
[다음: 이항분포 →](../binomial/index.qmd)
:::
