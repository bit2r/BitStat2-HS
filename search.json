[
  {
    "objectID": "pages/05_society/project/index.html",
    "href": "pages/05_society/project/index.html",
    "title": "통계적 탐구 프로젝트",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/05_society/project/index.html#탐구의-다섯-단계",
    "href": "pages/05_society/project/index.html#탐구의-다섯-단계",
    "title": "통계적 탐구 프로젝트",
    "section": "탐구의 다섯 단계",
    "text": "탐구의 다섯 단계\n통계적 탐구는 데이터를 그리는 기술이 아니라 질문에 답하는 절차입니다. 아래 다섯 단계를 순서대로 밟으면, 지금까지 배운 빛스탯 모듈이 각 단계의 도구가 됩니다.\n\n\n설문에서 이름·전화번호·주소 같은 개인정보는 수집하지 않습니다. 누가 답했는지 알 수 없게 익명으로 모으는 것이 원칙입니다.\n\n① 문제 설정\n막연한 궁금증을 측정 가능한 질문으로 바꿉니다. “우리 학교 급식은 어떤가?”가 아니라 “우리 학년 학생의 급식 만족도(5점 척도) 평균은 몇 점인가?”처럼 무엇을, 누구에게서, 어떤 단위로 잴지 정합니다. 질문이 구체적이어야 다음 단계가 모두 쉬워집니다.\n\n\n② 자료 수집 계획\n전체(모집단)를 다 조사할지, 일부(표본)만 뽑을지 정합니다. 표본을 쓴다면 특정 반이나 친한 친구만 조사하는 치우침이 없도록 뽑는 방법을 계획합니다. 몇 명을 조사할지, 설문 문항을 어떻게 쓸지도 이 단계에서 정합니다.\n\n\n③ 수집과 정리\n계획대로 자료를 모으고 표로 정리합니다. 빠진 응답이나 잘못 적힌 값이 없는지 확인하고, 변수마다 숫자형인지 범주형인지 구분해 둡니다. 정리한 자료를 표·그래프·요약값으로 살펴보는 연습은 데이터 탐색기에서 할 수 있습니다.\n\n\n④ 분석\n질문에 맞는 그래프와 요약값을 골라 자료가 말하는 것을 읽어냅니다. 그래프 선택 요령은 통계 그래프 만들기를, 표본으로 전체를 추정할 때의 오차 해석은 여론조사 읽기와 모비율의 추정을 참고하세요.\n\n\n⑤ 결론과 보고\n분석 결과를 처음 질문에 대한 답으로 정리해 보고서로 씁니다. 결론은 자료가 보여 주는 범위 안에서만 말하고, 표본의 한계(인원 수, 뽑은 방법, 조사 시기)를 함께 밝힙니다. 자료의 출처와 수집 방법을 적는 것도 보고의 일부입니다."
  },
  {
    "objectID": "pages/05_society/project/index.html#탐구-주제-예시",
    "href": "pages/05_society/project/index.html#탐구-주제-예시",
    "title": "통계적 탐구 프로젝트",
    "section": "탐구 주제 예시",
    "text": "탐구 주제 예시\n\n스마트폰 사용시간과 수면시간의 관계\n매점 대기줄 — 시간대별 대기 인원과 대기 시간\n급식 만족도 조사(5점 척도)와 메뉴별 비교\n교복 개선 설문 — 항목별 선호 비율\n등굣길 교통수단(도보/버스/자전거 등)별 통학시간 비교\n학년별 한 달 독서량 비교"
  },
  {
    "objectID": "pages/05_society/project/index.html#제출-전-점검",
    "href": "pages/05_society/project/index.html#제출-전-점검",
    "title": "통계적 탐구 프로젝트",
    "section": "제출 전 점검",
    "text": "제출 전 점검\n\n\n\n\n\n\n중요제출 전 점검 목록\n\n\n\n\n질문이 측정 가능한가?\n표본이 치우치지 않았는가?\n그래프 눈금이 정직한가?\n결론이 자료 범위를 넘지 않는가?\n자료 출처를 밝혔는가?"
  },
  {
    "objectID": "pages/05_society/project/index.html#평가-기준예시",
    "href": "pages/05_society/project/index.html#평가-기준예시",
    "title": "통계적 탐구 프로젝트",
    "section": "평가 기준(예시)",
    "text": "평가 기준(예시)\n\n\n\n\n\n\n\n\n단계\n우수\n보통\n\n\n\n\n문제 설정\n측정 가능한 구체적 질문\n질문이 막연하거나 측정 단위가 불분명\n\n\n자료 수집\n치우침 없는 표본 계획과 실행\n편의에 따라 일부만 조사\n\n\n분석\n질문에 맞는 그래프·요약값 선택\n그래프 종류가 질문과 어긋남\n\n\n보고\n자료 범위 안의 결론 + 한계·출처 명시\n자료를 넘어선 단정, 출처 누락\n\n\n\n\n← 이전: 여론조사 읽기 처음으로: 빛스탯 HS 홈 →"
  },
  {
    "objectID": "pages/05_society/make_chart/index.html",
    "href": "pages/05_society/make_chart/index.html",
    "title": "통계 그래프 만들기",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/05_society/make_chart/index.html#개념",
    "href": "pages/05_society/make_chart/index.html#개념",
    "title": "통계 그래프 만들기",
    "section": "개념",
    "text": "개념\n좋은 그래프는 질문에서 출발합니다. “분포의 모양이 어떤가?”에는 히스토그램, “두 집단이 다른가?”에는 상자그림, “두 변수가 함께 변하는가?”에는 산점도, “각 범주가 몇 개인가?”에는 막대그래프가 맞습니다. 그래프 종류를 먼저 고르는 게 아니라, 알고 싶은 질문을 먼저 정하고 거기에 맞는 그래프를 고르는 것이 순서입니다.\n\n\n3차원 효과를 넣거나 세로축 눈금을 0이 아닌 값에서 시작하는 그래프는 차이를 실제보다 부풀려 보이게 하는 잘못된 그래프의 대표 사례입니다.\n아래 앱은 데이터 탐색기와 같은 교육용 예시(가상) 데이터(고등학생 25명)를 씁니다. 그래프 종류와 변수를 바꿔 가며 각 그래프가 어떤 질문에 답하는지 확인해 보세요."
  },
  {
    "objectID": "pages/05_society/make_chart/index.html#만지며-배우기",
    "href": "pages/05_society/make_chart/index.html#만지며-배우기",
    "title": "통계 그래프 만들기",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 620\n\nfrom shiny import App, render, ui\nimport numpy as np\nimport matplotlib\nimport matplotlib.pyplot as plt\n\nmatplotlib.rcParams[\"font.size\"] = 10\n\nACCENT = \"#2563eb\"\nINK = \"#111827\"\nMUTED = \"#6b7280\"\n\n# 교육용 예시(가상) 데이터 — 고등학생 25명\nDATA = {\n    \"키(cm)\": [172.5, 158.3, 165.0, 181.2, 160.4, 175.8, 168.9, 155.2,\n               178.4, 162.7, 170.1, 152.8, 183.6, 166.3, 159.5, 174.2,\n               161.8, 169.4, 187.0, 157.6, 171.3, 164.5, 176.9, 153.9, 167.2],\n    \"통학시간(분)\": [15, 30, 10, 45, 25, 5, 60, 20, 35, 12, 40, 8, 55,\n                    18, 22, 70, 28, 14, 33, 48, 9, 26, 38, 16, 52],\n    \"수면시간(시간)\": [6.5, 7.2, 5.8, 6.0, 7.5, 8.0, 5.5, 6.8, 7.0, 6.2,\n                      8.5, 7.8, 5.2, 6.4, 7.1, 6.6, 8.2, 5.9, 6.1, 7.4,\n                      6.9, 5.6, 7.7, 6.3, 8.8],\n    \"성별\": [\"남\", \"여\", \"여\", \"남\", \"여\", \"남\", \"남\", \"여\", \"남\", \"여\",\n             \"남\", \"여\", \"남\", \"여\", \"여\", \"남\", \"여\", \"남\", \"남\", \"여\",\n             \"남\", \"여\", \"남\", \"여\", \"남\"],\n}\n\nNUM = [\"키(cm)\", \"통학시간(분)\", \"수면시간(시간)\"]\n\n# 그림 축 라벨은 영어만 사용 (한글 변수명 → 영어 라벨)\nAXIS = {\n    \"키(cm)\": \"height (cm)\",\n    \"통학시간(분)\": \"commute (min)\",\n    \"수면시간(시간)\": \"sleep (h)\",\n    \"성별\": \"gender\",\n}\n\nMSG = {\n    \"hist\": \"히스토그램은 '분포의 모양이 어떤가?'라는 질문에 맞는 그래프입니다.\",\n    \"box\": \"상자그림은 '두 집단이 다른가?'처럼 집단을 비교하는 질문에 맞습니다.\",\n    \"scatter\": \"산점도는 '두 변수가 함께 변하는가?'라는 관계 질문에 맞습니다.\",\n    \"bar\": \"막대그래프는 '각 범주가 몇 개인가?'라는 도수 질문에 맞습니다.\",\n}\n\ndef tufte(ax):\n    for side in (\"top\", \"right\"):\n        ax.spines[side].set_visible(False)\n    for side in (\"left\", \"bottom\"):\n        ax.spines[side].set_color(\"#9ca3af\")\n    ax.tick_params(colors=MUTED, labelsize=9)\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_select(\n            \"kind\", \"그래프 종류\",\n            {\"hist\": \"히스토그램 (숫자 1개)\",\n             \"box\": \"상자그림 (숫자 1개, 성별 비교)\",\n             \"scatter\": \"산점도 (숫자 2개)\",\n             \"bar\": \"막대그래프 (성별 도수)\"},\n        ),\n        ui.input_select(\"xvar\", \"x 변수\", {k: k for k in NUM}),\n        ui.input_select(\"yvar\", \"y 변수 (산점도에서만 사용)\",\n                        {k: k for k in NUM}, selected=\"수면시간(시간)\"),\n        width=240,\n    ),\n    ui.card(\n        ui.card_header(\"그래프\"),\n        ui.output_plot(\"chart\", height=\"340px\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @render.plot(alt=\"선택한 종류의 통계 그래프\")\n    def chart():\n        kind = input.kind()\n        xv = input.xvar()\n        fig, ax = plt.subplots()\n\n        if kind == \"hist\":\n            ax.hist(DATA[xv], bins=8, color=ACCENT, alpha=0.85,\n                    edgecolor=\"white\")\n            ax.set_xlabel(AXIS[xv], color=MUTED)\n            ax.set_ylabel(\"count\", color=MUTED)\n        elif kind == \"box\":\n            boys = [x for x, g in zip(DATA[xv], DATA[\"성별\"]) if g == \"남\"]\n            girls = [x for x, g in zip(DATA[xv], DATA[\"성별\"]) if g == \"여\"]\n            ax.boxplot([boys, girls])\n            ax.set_xticks([1, 2])\n            ax.set_xticklabels([\"M\", \"F\"])\n            ax.set_xlabel(\"gender\", color=MUTED)\n            ax.set_ylabel(AXIS[xv], color=MUTED)\n        elif kind == \"scatter\":\n            yv = input.yvar()\n            ax.scatter(DATA[xv], DATA[yv], color=ACCENT, alpha=0.8, s=28)\n            ax.set_xlabel(AXIS[xv], color=MUTED)\n            ax.set_ylabel(AXIS[yv], color=MUTED)\n        else:  # bar\n            counts = [DATA[\"성별\"].count(\"남\"), DATA[\"성별\"].count(\"여\")]\n            ax.bar([\"M\", \"F\"], counts, color=\"#9ca3af\", width=0.5)\n            ax.set_xlabel(\"gender\", color=MUTED)\n            ax.set_ylabel(\"count\", color=MUTED)\n\n        tufte(ax)\n        return fig\n\n    @render.ui\n    def readout():\n        line = (MSG[input.kind()] + \" 정리하면 — 분포의 모양은 히스토그램, \"\n                \"집단 비교는 상자그림, 두 변수 관계는 산점도, \"\n                \"범주 개수는 막대그래프입니다.\")\n        return ui.HTML(f'&lt;p class=\"hs-readline\"&gt;{line}&lt;/p&gt;')\n\napp = App(app_ui, server)\n\n\nmatplotlib에서 그래프 종류는 함수 하나 차이입니다.\nimport matplotlib.pyplot as plt\n\nheight = [172.5, 158.3, 165.0, 181.2, 160.4]   # 일부만 표시\nsleep = [6.5, 7.2, 5.8, 6.0, 7.5]\n\nfig, ax = plt.subplots()\nax.hist(height, bins=8)              # 히스토그램: 분포의 모양\n# ax.boxplot([boys, girls])          # 상자그림: 집단 비교\n# ax.scatter(height, sleep)          # 산점도: 두 변수 관계\n# ax.bar([\"M\", \"F\"], [13, 12])       # 막대그래프: 범주 도수\nplt.show()"
  },
  {
    "objectID": "pages/05_society/make_chart/index.html#스스로-확인",
    "href": "pages/05_society/make_chart/index.html#스스로-확인",
    "title": "통계 그래프 만들기",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — ’남학생과 여학생의 키가 얼마나 다른가?’를 보이려면 어떤 그래프가 맞는가?\n\n\n\n\n\n집단 비교 질문이므로 상자그림입니다. 앱에서 상자그림을 고르고 x 변수를 키(cm)로 두면, 두 집단의 중앙값·퍼짐·범위를 한눈에 비교할 수 있습니다. 히스토그램 하나로는 두 집단이 섞여 보이지 않습니다.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — 통학시간과 수면시간의 관계를 산점도로 그려 보라. 어떤 경향이 보이는가?\n\n\n\n\n\n산점도에서 x 변수를 통학시간(분), y 변수를 수면시간(시간)으로 고르면 통학시간이 긴 학생일수록 수면시간이 짧은 음의 경향이 어렴풋이 보입니다. 다만 25명뿐인 가상 데이터이므로, 이런 경향이 진짜인지는 더 많은 자료로 확인해야 합니다.\n\n\n\n\n← 이전: 데이터 탐색기 다음: 여론조사 읽기 →"
  },
  {
    "objectID": "pages/04_infer/sampling/index.html",
    "href": "pages/04_infer/sampling/index.html",
    "title": "표본추출",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/04_infer/sampling/index.html#개념",
    "href": "pages/04_infer/sampling/index.html#개념",
    "title": "표본추출",
    "section": "개념",
    "text": "개념\n조사 대상 전체(모집단)를 빠짐없이 조사하는 것이 전수조사, 그 일부(표본)만 뽑아 조사하는 것이 표본조사입니다. 전교생 400명의 평균 키를 알고 싶을 때, 표본 \\(n\\)명을 잘 뽑으면 표본평균 \\(\\bar{x}\\)로 모평균 \\(m\\)을 추정할 수 있습니다.\n\\[\\text{모집단(크기 } N=400\\text{)} \\;\\longrightarrow\\; \\text{표본(크기 } n\\text{)} \\;\\longrightarrow\\; \\bar{x} \\approx m\\]\n\n\n급식 만족도 조사를 한 반에서만 하면 안 되는 이유 — 그 반의 특성(급식실과의 거리, 학년 분위기 등)이 결과를 치우치게 하기 때문입니다. 표본은 모집단 전체를 골고루 대표해야 합니다.\n표본을 뽑는 대표적인 방법은 세 가지입니다. 단순임의추출은 제비뽑기처럼 모두에게 같은 확률을 주고, 계통추출은 명부에서 일정한 간격으로 뽑으며, 층화추출은 남/녀처럼 집단을 층으로 나눈 뒤 각 층에서 비례하여 뽑습니다. 아래 앱에서 방법을 바꿔 가며 어떤 점들이 뽑히는지, 표본평균이 모평균에서 얼마나 벗어나는지 관찰해 보세요."
  },
  {
    "objectID": "pages/04_infer/sampling/index.html#만지며-배우기",
    "href": "pages/04_infer/sampling/index.html#만지며-배우기",
    "title": "표본추출",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 640\n\nfrom shiny import App, reactive, render, ui\nimport numpy as np\nimport matplotlib\nimport matplotlib.pyplot as plt\n\nmatplotlib.rcParams[\"font.size\"] = 10\n\nACCENT = \"#2563eb\"\nINK = \"#111827\"\nMUTED = \"#6b7280\"\n\ndef tufte(ax):\n    for side in (\"top\", \"right\"):\n        ax.spines[side].set_visible(False)\n    for side in (\"left\", \"bottom\"):\n        ax.spines[side].set_color(\"#9ca3af\")\n    ax.tick_params(colors=MUTED, labelsize=9)\n\n# 모집단: 전교생 400명의 키 (고정된 시드 → 항상 같은 학교)\nrng = np.random.default_rng(42)\nmale = np.clip(rng.normal(173, 6, 200), 145, 195)    # 앞 200명: 남\nfemale = np.clip(rng.normal(161, 5, 200), 145, 195)  # 뒤 200명: 여\nPOP = np.concatenate([male, female])\nPOP_MEAN = POP.mean()\n\nMETHOD_NOTE = {\n    \"srs\": \"단순임의추출은 모두에게 같은 확률을 주는 제비뽑기입니다. 어디에도 치우치지 않지만, 우연히 한쪽 층이 많이 뽑힐 수도 있습니다.\",\n    \"sys\": \"계통추출은 명부에서 일정 간격으로 뽑아 빠르고 간편합니다. 다만 명부에 규칙적인 배열이 있으면 치우칠 수 있습니다.\",\n    \"strat\": \"층화추출은 남녀 비율(200:200)을 표본에서도 그대로 유지하므로, 남녀 키 차이 때문에 생기는 치우침이 적습니다.\",\n}\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_select(\n            \"method\", \"추출 방법\",\n            {\"srs\": \"단순임의추출\", \"sys\": \"계통추출\", \"strat\": \"층화추출(남/녀)\"},\n        ),\n        ui.input_slider(\"n\", \"표본 크기 n\", min=10, max=100, value=40, step=10),\n        ui.input_action_button(\"redraw\", \"다시 추출\"),\n        width=240,\n    ),\n    ui.card(\n        ui.card_header(\"전교생 400명(회색) 중 이번에 뽑힌 표본(파랑)\"),\n        ui.output_plot(\"grid_plot\", height=\"340px\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @reactive.calc\n    def sample_idx():\n        input.redraw()  # 버튼을 누르면 새로 추출\n        n = input.n()\n        method = input.method()\n        if method == \"srs\":\n            return np.random.choice(400, size=n, replace=False)\n        if method == \"sys\":\n            k = 400 // n\n            start = np.random.randint(0, k)\n            return np.arange(start, 400, k)[:n]\n        # 층화추출: 남녀에서 절반씩\n        half = n // 2\n        idx_m = np.random.choice(200, size=half, replace=False)\n        idx_f = 200 + np.random.choice(200, size=n - half, replace=False)\n        return np.concatenate([idx_m, idx_f])\n\n    @render.plot(alt=\"모집단 격자와 추출된 표본\")\n    def grid_plot():\n        idx = sample_idx()\n        all_i = np.arange(400)\n        xs, ys = all_i % 20, all_i // 20  # 행 우선 배치: 0~9행 남, 10~19행 여\n\n        fig, ax = plt.subplots()\n        ax.scatter(xs, ys, s=18, color=\"#d1d5db\")\n        ax.scatter(xs[idx], ys[idx], s=42, color=ACCENT, zorder=3)\n        ax.text(-1.8, 4.5, \"M\", color=MUTED, ha=\"center\", va=\"center\", fontsize=11)\n        ax.text(-1.8, 14.5, \"F\", color=MUTED, ha=\"center\", va=\"center\", fontsize=11)\n        ax.axhline(9.5, color=\"#9ca3af\", linewidth=0.7, linestyle=\":\")\n        ax.invert_yaxis()\n        ax.set_xticks([])\n        ax.set_yticks([])\n        ax.set_aspect(\"equal\")\n        tufte(ax)\n        return fig\n\n    @render.ui\n    def readout():\n        xbar = POP[sample_idx()].mean()\n        return ui.HTML(\n            f'&lt;p class=\"hs-readline\"&gt;'\n            f\"모평균 m = {POP_MEAN:.2f} cm · 이번 표본평균 x̄ = {xbar:.2f} cm · \"\n            f\"차이 = {abs(POP_MEAN - xbar):.2f} cm. \"\n            f\"{METHOD_NOTE[input.method()]}&lt;/p&gt;\"\n        )\n\napp = App(app_ui, server)\n\n\n세 가지 추출 방법의 핵심은 인덱스를 고르는 방식의 차이입니다.\nimport numpy as np\n\npop = np.arange(400)                       # 전교생 400명의 번호\n\nsrs = np.random.choice(400, 40, replace=False)   # 단순임의추출\n\nk = 400 // 40                                    # 계통추출: 간격 10\nsys_ = np.arange(np.random.randint(k), 400, k)\n\nboys = np.random.choice(200, 20, replace=False)  # 층화추출: 남 20 + 여 20\ngirls = 200 + np.random.choice(200, 20, replace=False)\nstrat = np.concatenate([boys, girls])"
  },
  {
    "objectID": "pages/04_infer/sampling/index.html#스스로-확인",
    "href": "pages/04_infer/sampling/index.html#스스로-확인",
    "title": "표본추출",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — 단순임의추출로 ’다시 추출’을 여러 번 눌러 보라. 표본평균이 매번 달라지는 이유는?\n\n\n\n\n\n어떤 400명 중 누가 뽑히느냐가 매번 달라지기 때문입니다. 표본평균 \\(\\bar{x}\\)는 표본에 따라 값이 변하는 확률변수입니다 — 이 사실이 다음 모듈 “표본평균의 분포”의 출발점입니다.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — n = 20처럼 표본이 작을 때 층화추출이 단순임의추출보다 유리한 이유는?\n\n\n\n\n\n단순임의추출은 우연히 남학생(또는 여학생)만 많이 뽑힐 수 있고, 남녀 키 차이가 약 12 cm이므로 표본평균이 크게 치우칩니다. 층화추출은 남녀를 10명씩 강제로 나누어 뽑아 이런 치우침을 원천 차단합니다. 앱에서 두 방법의 “차이” 값을 여러 번 비교해 보세요.\n\n\n\n\n← 이전: 정규분포와 표준화 다음: 표본평균의 분포 →"
  },
  {
    "objectID": "pages/04_infer/ci_prop/index.html",
    "href": "pages/04_infer/ci_prop/index.html",
    "title": "모비율의 추정",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/04_infer/ci_prop/index.html#개념",
    "href": "pages/04_infer/ci_prop/index.html#개념",
    "title": "모비율의 추정",
    "section": "개념",
    "text": "개념\n찬성 비율, 지지율처럼 모집단에서 어떤 성질을 가진 비율 \\(p\\)(모비율)를 추정할 때는, \\(n\\)명을 조사해 얻은 표본비율 \\(\\hat{p}\\)을 사용합니다. \\(n\\)이 충분히 크면 모비율 \\(p\\)의 신뢰구간은 다음과 같습니다.\n\\[\\hat{p} - z\\sqrt{\\frac{\\hat{p}(1-\\hat{p})}{n}} \\;\\le\\; p \\;\\le\\;\n\\hat{p} + z\\sqrt{\\frac{\\hat{p}(1-\\hat{p})}{n}}\n\\qquad (z = 1.96,\\ 2.576)\\]\n\n\n여기서 익힌 오차범위 읽기는 다음 모듈(05 여론조사 읽기)에서 실제 뉴스 기사 해석으로 이어집니다.\n\\(\\pm\\) 뒤의 값 \\(z\\sqrt{\\hat{p}(1-\\hat{p})/n}\\)이 뉴스에서 말하는 오차범위입니다. “지지율 52%, 오차범위 ±3.1%p”라면 진짜 지지율은 48.9%~55.1% 어디쯤이라는 뜻이므로, 구간이 50%를 걸치면 누가 앞선다고 단정할 수 없습니다. 아래 앱에서 \\(n\\)과 \\(\\hat{p}\\)을 움직여 오차범위가 어떻게 변하는지 확인해 보세요."
  },
  {
    "objectID": "pages/04_infer/ci_prop/index.html#만지며-배우기",
    "href": "pages/04_infer/ci_prop/index.html#만지며-배우기",
    "title": "모비율의 추정",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 620\n\nfrom math import sqrt\nfrom shiny import App, render, ui\nimport numpy as np\nimport matplotlib\nimport matplotlib.pyplot as plt\n\nmatplotlib.rcParams[\"font.size\"] = 10\n\nACCENT = \"#2563eb\"\nINK = \"#111827\"\nMUTED = \"#6b7280\"\n\ndef tufte(ax):\n    for side in (\"top\", \"right\"):\n        ax.spines[side].set_visible(False)\n    for side in (\"left\", \"bottom\"):\n        ax.spines[side].set_color(\"#9ca3af\")\n    ax.tick_params(colors=MUTED, labelsize=9)\n\nZ = {\"95\": 1.96, \"99\": 2.576}\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_slider(\"n\", \"응답자 수 n\", min=100, max=3000, value=1000, step=100),\n        ui.input_slider(\"phat\", \"표본비율 p̂\", min=0.10, max=0.90, value=0.52, step=0.01),\n        ui.input_select(\"level\", \"신뢰수준\", {\"95\": \"95%\", \"99\": \"99%\"}),\n        width=240,\n    ),\n    ui.card(\n        ui.card_header(\"모비율 p의 신뢰구간 — 점선은 50% 기준선\"),\n        ui.output_plot(\"ci_plot\", height=\"340px\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @render.plot(alt=\"모비율의 신뢰구간 수직선\")\n    def ci_plot():\n        n, phat = input.n(), input.phat()\n        z = Z[input.level()]\n        margin = z * sqrt(phat * (1 - phat) / n)\n        lo, hi = phat - margin, phat + margin\n\n        fig, ax = plt.subplots()\n        ax.hlines(0, lo, hi, color=ACCENT, linewidth=6, alpha=0.85)\n        ax.plot([phat], [0], \"o\", color=ACCENT, markersize=9, zorder=3)\n        ax.annotate(\"p-hat\", (phat, 0), xytext=(phat, 0.45),\n                    ha=\"center\", color=INK, fontsize=10)\n        ax.axvline(0.5, color=INK, linewidth=1, linestyle=\"--\")\n        ax.set_xlim(max(0.0, phat - 5 * margin), min(1.0, phat + 5 * margin))\n        ax.set_ylim(-1, 1)\n        ax.set_yticks([])\n        ax.set_xlabel(\"p\", color=MUTED)\n        tufte(ax)\n        return fig\n\n    @render.ui\n    def readout():\n        n, phat = input.n(), input.phat()\n        level = input.level()\n        z = Z[level]\n        margin = z * sqrt(phat * (1 - phat) / n)\n        lo, hi = phat - margin, phat + margin\n        if lo &lt;= 0.5 &lt;= hi:\n            verdict = (\"구간이 50%를 걸치므로 절반보다 많다(우세)고 \"\n                       \"단정할 수 없습니다.\")\n        elif lo &gt; 0.5:\n            verdict = \"구간 전체가 50%보다 위에 있어 절반을 넘는다고 볼 수 있습니다.\"\n        else:\n            verdict = \"구간 전체가 50%보다 아래에 있어 절반에 못 미친다고 볼 수 있습니다.\"\n        return ui.HTML(\n            f'&lt;p class=\"hs-readline\"&gt;'\n            f\"표본비율 {phat * 100:.1f}%, {level}% 신뢰구간 \"\n            f\"[{lo * 100:.1f}%, {hi * 100:.1f}%] — 오차범위 ±{margin * 100:.1f}%p. \"\n            f\"n을 4배로 늘리면 오차범위는 절반이 됩니다. {verdict}&lt;/p&gt;\"\n        )\n\napp = App(app_ui, server)\n\n\n여론조사 기사의 오차범위는 이 한 줄 계산에서 나옵니다.\nfrom math import sqrt\n\nn, phat, z = 1000, 0.52, 1.96          # 응답자 1000명, 표본비율 52%, 95%\n\nmargin = z * sqrt(phat * (1 - phat) / n)   # 오차범위 ≈ 0.031 (±3.1%p)\nlo, hi = phat - margin, phat + margin\n\nprint(f\"{lo:.3f} ~ {hi:.3f}\")              # 0.489 ~ 0.551 → 50%를 걸친다"
  },
  {
    "objectID": "pages/04_infer/ci_prop/index.html#스스로-확인",
    "href": "pages/04_infer/ci_prop/index.html#스스로-확인",
    "title": "모비율의 추정",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — n = 1000, p̂ = 0.52, 95%일 때 ’우세’라고 단정할 수 없는 이유는? n이 얼마면 달라지는가?\n\n\n\n\n\n오차범위가 약 \\(\\pm 3.1\\)%p라 구간 \\([48.9\\%, 55.1\\%]\\)이 50%를 걸치기 때문입니다. 앱에서 \\(n\\)을 키워 보면 \\(n = 2500\\) 근처부터 구간의 아래끝이 50%를 넘어섭니다(\\(\\pm 2.0\\)%p 이하). 오차범위는 \\(\\sqrt{n}\\)에 반비례하므로 \\(n\\)을 4배로 하면 절반이 됩니다.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — n을 고정하고 p̂을 0.1 → 0.5 → 0.9로 움직여 보라. 오차범위가 가장 큰 곳은?\n\n\n\n\n\n\\(\\hat{p} = 0.5\\)에서 가장 큽니다. \\(\\hat{p}(1-\\hat{p})\\)이 \\(0.5 \\times 0.5 = 0.25\\)로 최대가 되기 때문입니다. 그래서 여론조사 기관은 안전하게 \\(\\hat{p} = 0.5\\)를 가정한 최대 오차범위를 함께 표기하곤 합니다.\n\n\n\n\n← 이전: 모평균의 신뢰구간 다음: 데이터 탐색기 →"
  },
  {
    "objectID": "pages/03_dist/random_var/index.html",
    "href": "pages/03_dist/random_var/index.html",
    "title": "확률변수와 기댓값",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/03_dist/random_var/index.html#개념",
    "href": "pages/03_dist/random_var/index.html#개념",
    "title": "확률변수와 기댓값",
    "section": "개념",
    "text": "개념\n어떤 시행의 결과에 따라 값이 정해지는 변수를 확률변수 \\(X\\)라 하고, \\(X\\)가 가질 수 있는 각 값에 확률을 대응시킨 것을 확률분포라고 합니다. 확률변수 \\(X\\)의 기댓값(평균)은 각 값에 그 확률을 곱해 모두 더한 값입니다.\n\\[E(X) = x_1 p_1 + x_2 p_2 + \\cdots + x_n p_n\\]\n\n\n복권 한 장의 “평균 당첨금”이 바로 기댓값입니다. 아래 앱에서 복권 시나리오를 골라 기댓값과 복권 가격을 비교해 보세요.\n시행을 많이 반복하면 실제 관측값들의 평균(표본평균)은 기댓값에 가까워집니다. 아래 앱에서 시행 횟수를 늘려가며 두 값이 가까워지는 모습을 직접 확인해 보세요."
  },
  {
    "objectID": "pages/03_dist/random_var/index.html#만지며-배우기",
    "href": "pages/03_dist/random_var/index.html#만지며-배우기",
    "title": "확률변수와 기댓값",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 640\n\nfrom shiny import App, reactive, render, ui\nimport numpy as np\nimport matplotlib\nimport matplotlib.pyplot as plt\n\nmatplotlib.rcParams[\"font.size\"] = 10\n\nACCENT = \"#2563eb\"\nINK = \"#111827\"\nMUTED = \"#6b7280\"\n\nSCENARIOS = {\n    \"die\": {\"name\": \"주사위 한 개의 눈\",\n            \"x\": [1, 2, 3, 4, 5, 6],\n            \"p\": [1/6] * 6},\n    \"coins\": {\"name\": \"동전 두 개의 앞면 수\",\n              \"x\": [0, 1, 2],\n              \"p\": [0.25, 0.5, 0.25]},\n    \"lotto\": {\"name\": \"복권 한 장의 당첨금(원)\",\n              \"x\": [0, 1000, 10000, 1000000],\n              \"p\": [0.899, 0.09, 0.01, 0.001]},\n}\n\ndef tufte(ax):\n    for side in (\"top\", \"right\"):\n        ax.spines[side].set_visible(False)\n    for side in (\"left\", \"bottom\"):\n        ax.spines[side].set_color(\"#9ca3af\")\n    ax.tick_params(colors=MUTED, labelsize=9)\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_select(\n            \"scenario\", \"확률변수 X\",\n            {k: v[\"name\"] for k, v in SCENARIOS.items()},\n        ),\n        ui.input_slider(\"n\", \"시행 횟수 n\", min=10, max=5000, value=100, step=10),\n        ui.input_action_button(\"redraw\", \"다시 추첨\"),\n        width=240,\n    ),\n    ui.card(\n        ui.card_header(\"확률분포(테두리) vs 실제 시행의 상대도수(파랑)\"),\n        ui.output_plot(\"dist_plot\", height=\"340px\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @reactive.calc\n    def draws():\n        input.redraw()  # 버튼을 누르면 새로 추첨\n        sc = SCENARIOS[input.scenario()]\n        idx = np.random.choice(len(sc[\"x\"]), size=input.n(), p=sc[\"p\"])\n        return np.array(sc[\"x\"])[idx]\n\n    @render.plot(alt=\"확률분포와 상대도수 비교\")\n    def dist_plot():\n        sc = SCENARIOS[input.scenario()]\n        vals = draws()\n        pos = np.arange(len(sc[\"x\"]))\n        freq = [np.mean(vals == v) for v in sc[\"x\"]]\n\n        fig, ax = plt.subplots()\n        ax.bar(pos - 0.2, sc[\"p\"], width=0.38, fill=False,\n               edgecolor=INK, linewidth=1.2, label=\"P(X=x)\")\n        ax.bar(pos + 0.2, freq, width=0.38, color=ACCENT, alpha=0.85,\n               label=\"relative freq.\")\n        ax.set_xticks(pos)\n        ax.set_xticklabels([f\"{v:,}\" for v in sc[\"x\"]])\n        ax.set_ylabel(\"P(X = x)\", color=MUTED)\n        ax.legend(frameon=False, fontsize=9)\n        tufte(ax)\n        return fig\n\n    @render.ui\n    def readout():\n        sc = SCENARIOS[input.scenario()]\n        e = sum(x * p for x, p in zip(sc[\"x\"], sc[\"p\"]))\n        m = draws().mean()\n        line = (f\"기댓값 E(X) = {e:,.1f} · \"\n                f\"시행 {input.n():,}번의 표본평균 = {m:,.1f} · \"\n                f\"차이 = {abs(e - m):,.1f}\")\n        extra = \"\"\n        if input.scenario() == \"lotto\":\n            extra = (\" — 복권 한 장의 기댓값은 약 1,190원입니다. \"\n                     \"가격이 2,000원이라면 살수록 평균적으로 손해입니다.\")\n        return ui.HTML(f'&lt;p class=\"hs-readline\"&gt;{line}{extra}&lt;/p&gt;')\n\napp = App(app_ui, server)\n\n\n앱의 핵심 계산은 이 몇 줄이 전부입니다.\nimport numpy as np\n\nx = [0, 1000, 10000, 1000000]          # 복권 당첨금\np = [0.899, 0.09, 0.01, 0.001]         # 각 확률\n\nE = sum(xi * pi for xi, pi in zip(x, p))   # 기댓값 E(X) = 1190.0\n\ndraws = np.random.choice(x, size=5000, p=p)  # 5000번 시행\nprint(E, draws.mean())                       # 기댓값 vs 표본평균"
  },
  {
    "objectID": "pages/03_dist/random_var/index.html#스스로-확인",
    "href": "pages/03_dist/random_var/index.html#스스로-확인",
    "title": "확률변수와 기댓값",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — 시행 횟수를 10에서 5000까지 늘리면 표본평균은 어떻게 변하는가?\n\n\n\n\n\n표본평균이 기댓값 \\(E(X)\\)에 점점 가까워집니다. 시행 횟수가 적을 때는 추첨할 때마다 표본평균이 크게 출렁이지만, 횟수가 커지면 안정됩니다. (대수의 법칙 — 04단원 표본평균의 분포에서 다시 만납니다.)\n\n\n\n\n\n\n\n\n\n힌트문제 2 — 복권 시나리오에서 ’다시 추첨’을 여러 번 눌러 보라. n=100일 때 표본평균이 유독 크게 튀는 이유는?\n\n\n\n\n\n당첨금 1,000,000원의 확률이 0.001이라 100번 추첨에는 대개 한 번도 나오지 않다가, 어쩌다 한 번 나오면 표본평균이 크게 뜁니다. 드문 값이 평균에 미치는 영향을 눈으로 확인해 보세요.\n\n\n\n\n← 이전: 조건부확률과 독립 다음: 이항분포 →"
  },
  {
    "objectID": "pages/03_dist/continuous_rv/index.html",
    "href": "pages/03_dist/continuous_rv/index.html",
    "title": "연속확률변수와 확률분포",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/03_dist/continuous_rv/index.html#개념",
    "href": "pages/03_dist/continuous_rv/index.html#개념",
    "title": "연속확률변수와 확률분포",
    "section": "개념",
    "text": "개념\n주사위 눈처럼 값이 뚝뚝 떨어지는 변수는 이산확률변수였습니다. 반면 대기 시간·키·무게처럼 어떤 구간의 값을 연속적으로 가질 수 있는 변수가 연속확률변수입니다. 연속확률변수는 값이 무수히 많아 한 점의 확률을 따질 수 없으므로, 확률을 넓이로 나타냅니다.\n이때 쓰는 함수가 확률밀도함수 \\(f(x)\\)이고, 다음 두 성질을 만족합니다.\n\\[f(x)\\ge 0,\\qquad \\int_{-\\infty}^{\\infty} f(x)\\,dx = 1\\]\n\n\n막대그래프의 “높이=확률”이 연속에서는 “넓이=확률”로 바뀝니다. 그래서 \\(f(x)\\) 자체는 확률이 아니라 밀도이고, 값이 1보다 클 수도 있습니다. 확률은 언제나 구간에 대한 넓이로만 정해집니다.\n확률변수 \\(X\\)가 구간 \\([a,\\,b]\\)의 값을 가질 확률은 그 구간에서 밀도함수 아래의 넓이입니다.\n\\[\\mathrm{P}(a\\le X\\le b) = \\int_{a}^{b} f(x)\\,dx\\]\n아래 앱은 삼각형 모양의 밀도함수를 씁니다. 구간 \\([a,\\,b]\\)를 움직이면 색칠된 넓이가 곧 그 구간의 확률이고, 전체 넓이는 항상 1임을 확인해 보세요."
  },
  {
    "objectID": "pages/03_dist/continuous_rv/index.html#만지며-배우기",
    "href": "pages/03_dist/continuous_rv/index.html#만지며-배우기",
    "title": "연속확률변수와 확률분포",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 660\n\nfrom shiny import App, render, ui\nimport numpy as np\nimport matplotlib\nimport matplotlib.pyplot as plt\n\nmatplotlib.rcParams[\"font.size\"] = 10\nACCENT = \"#2563eb\"\nINK = \"#111827\"\nMUTED = \"#6b7280\"\n\nL = 6.0          # X 의 범위: 0 ~ 6\nPEAK = 3.0       # 삼각형의 꼭짓점\n\ndef tufte(ax):\n    for side in (\"top\", \"right\"):\n        ax.spines[side].set_visible(False)\n    for side in (\"left\", \"bottom\"):\n        ax.spines[side].set_color(\"#9ca3af\")\n    ax.tick_params(colors=MUTED, labelsize=9)\n\ndef density(x):\n    # 밑변 L, 넓이 1 인 이등변 삼각형 밀도 (꼭짓점 높이 = 2/L)\n    x = np.asarray(x, dtype=float)\n    h = 2.0 / L\n    left = h * (x / PEAK)\n    right = h * ((L - x) / (L - PEAK))\n    f = np.where(x &lt; PEAK, left, right)\n    return np.clip(f, 0, None) * ((x &gt;= 0) & (x &lt;= L))\n\ndef area(x, y):\n    # 사다리꼴 적분 (numpy 버전과 무관하게 동작)\n    return float(np.sum((y[:-1] + y[1:]) * 0.5 * np.diff(x)))\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_slider(\"rng\", \"구간 [a, b]\", min=0.0, max=6.0,\n                        value=[2.0, 4.0], step=0.1),\n        width=250,\n    ),\n    ui.card(\n        ui.card_header(\"확률밀도함수 — 색칠한 넓이가 곧 확률입니다\"),\n        ui.output_plot(\"dens\", height=\"340px\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @render.plot(alt=\"삼각형 확률밀도함수와 구간의 넓이\")\n    def dens():\n        a, b = input.rng()\n        xs = np.linspace(0, L, 400)\n        ys = density(xs)\n\n        fig, ax = plt.subplots()\n        ax.plot(xs, ys, color=INK, linewidth=1.6)\n        mask = (xs &gt;= a) & (xs &lt;= b)\n        ax.fill_between(xs[mask], ys[mask], color=ACCENT, alpha=0.35)\n        ax.set_xlabel(\"x\", color=MUTED)\n        ax.set_ylabel(\"f(x)\", color=MUTED)\n        ax.set_ylim(0, 2.0 / L * 1.2)\n        tufte(ax)\n        return fig\n\n    @render.ui\n    def readout():\n        a, b = input.rng()\n        grid = np.linspace(a, b, 400)\n        prob = area(grid, density(grid)) if b &gt; a else 0.0\n        full = np.linspace(0, L, 800)\n        total = area(full, density(full))\n        return ui.HTML(\n            '&lt;p class=\"hs-readline\"&gt;'\n            f\"P({a:.1f} ≤ X ≤ {b:.1f}) = 색칠한 넓이 ≈ &lt;b&gt;{prob:.3f}&lt;/b&gt; 입니다. \"\n            f\"밀도함수 전체 넓이는 {total:.3f} ≈ 1 로, 어떤 구간을 잡아도 확률의 \"\n            \"합은 1을 넘지 않습니다. 구간을 넓히면 넓이가 커지고, a와 b를 붙이면 \"\n            \"넓이가 0에 가까워집니다 — 연속확률변수에서 한 점의 확률이 0인 이유입니다.&lt;/p&gt;\"\n        )\n\napp = App(app_ui, server)\n\n\n넓이(확률)는 사다리꼴 적분으로 간단히 구합니다.\nimport numpy as np\n\nL, PEAK = 6.0, 3.0\ndef f(x):\n    h = 2.0 / L\n    return np.where(x &lt; PEAK, h * x / PEAK, h * (L - x) / (L - PEAK))\n\ndef area(x, y):                      # 사다리꼴 적분\n    return np.sum((y[:-1] + y[1:]) * 0.5 * np.diff(x))\n\nx = np.linspace(2, 4, 400)           # 구간 [2, 4]\nprint(area(x, f(x)))                 # P(2 ≤ X ≤ 4) ≈ 0.556\n\nx_all = np.linspace(0, L, 800)\nprint(area(x_all, f(x_all)))         # 전체 넓이 ≈ 1"
  },
  {
    "objectID": "pages/03_dist/continuous_rv/index.html#스스로-확인",
    "href": "pages/03_dist/continuous_rv/index.html#스스로-확인",
    "title": "연속확률변수와 확률분포",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — 연속확률변수에서 P(X = 3) 은 얼마인가?\n\n\n\n\n\n정확히 0입니다. 한 점은 폭이 없어 넓이가 0이기 때문입니다. 연속확률변수의 확률은 언제나 구간(넓이)으로만 정해지므로, \\(\\mathrm{P}(X=3)\\)이 아니라 \\(\\mathrm{P}(2.9\\le X\\le 3.1)\\)처럼 구간으로 물어야 의미가 있습니다.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — 밀도함수 f(x) 의 값이 1보다 커도 되는가?\n\n\n\n\n\n됩니다. \\(f(x)\\)는 확률이 아니라 밀도이기 때문입니다. 이 앱의 삼각형은 꼭짓점 높이가 \\(\\tfrac{2}{6}\\approx0.33\\)이지만, 범위가 좁은 분포라면 높이가 1을 훌쩍 넘을 수도 있습니다. 제약은 오직 “\\(f(x)\\ge0\\)이고 전체 넓이가 1”이라는 것뿐입니다.\n\n\n\n\n← 이전: 이항분포 다음: 정규분포와 표준화 →"
  },
  {
    "objectID": "pages/02_prob/prob_basic/index.html",
    "href": "pages/02_prob/prob_basic/index.html",
    "title": "확률의 뜻",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/02_prob/prob_basic/index.html#개념",
    "href": "pages/02_prob/prob_basic/index.html#개념",
    "title": "확률의 뜻",
    "section": "개념",
    "text": "개념\n각 결과가 일어날 가능성이 모두 같은 시행에서, 사건 \\(A\\)의 수학적 확률은 전체 경우의 수에 대한 사건 \\(A\\)의 경우의 수의 비율입니다.\n\\[P(A) = \\frac{n(A)}{n(S)}\\]\n\n\n“시행을 늘릴수록 상대도수가 확률에 가까워진다”는 대수의 법칙은 04단원 표본평균의 분포에서 다시 만납니다.\n한편 실제로 시행을 \\(n\\)번 반복했을 때 사건 \\(A\\)가 일어난 상대도수 \\(\\dfrac{r_n}{n}\\)은 \\(n\\)이 커질수록 일정한 값에 가까워지는데, 이 값을 통계적 확률이라고 합니다. 동전 앞면처럼 수학적 확률을 아는 시행에서는 상대도수가 바로 그 수학적 확률로 다가갑니다. 아래 앱에서 시행 횟수를 늘려가며 파란 곡선(상대도수)이 점선(수학적 확률)에 달라붙는 모습을 확인해 보세요."
  },
  {
    "objectID": "pages/02_prob/prob_basic/index.html#만지며-배우기",
    "href": "pages/02_prob/prob_basic/index.html#만지며-배우기",
    "title": "확률의 뜻",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 640\n\nfrom shiny import App, reactive, render, ui\nimport numpy as np\nimport matplotlib\nimport matplotlib.pyplot as plt\n\nmatplotlib.rcParams[\"font.size\"] = 10\n\nACCENT = \"#2563eb\"\nINK = \"#111827\"\nMUTED = \"#6b7280\"\n\nTRIALS = {\n    \"coin\": {\"name\": \"동전 던지기 — 앞면\", \"p\": 0.5, \"label\": \"1/2\"},\n    \"die\": {\"name\": \"주사위 던지기 — 6의 눈\", \"p\": 1 / 6, \"label\": \"1/6\"},\n}\n\ndef tufte(ax):\n    for side in (\"top\", \"right\"):\n        ax.spines[side].set_visible(False)\n    for side in (\"left\", \"bottom\"):\n        ax.spines[side].set_color(\"#9ca3af\")\n    ax.tick_params(colors=MUTED, labelsize=9)\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_select(\n            \"trial\", \"시행 종류\",\n            {k: v[\"name\"] for k, v in TRIALS.items()},\n        ),\n        ui.input_slider(\"n\", \"시행 횟수 n\", min=10, max=10000, value=200, step=10),\n        ui.input_action_button(\"redraw\", \"다시 시행\"),\n        width=240,\n    ),\n    ui.card(\n        ui.card_header(\"상대도수의 변화(파랑) vs 수학적 확률(점선)\"),\n        ui.output_plot(\"freq_plot\", height=\"340px\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @reactive.calc\n    def draws():\n        input.redraw()  # 버튼을 누르면 새로 시행\n        p = TRIALS[input.trial()][\"p\"]\n        return (np.random.random(input.n()) &lt; p).astype(float)\n\n    @render.plot(alt=\"시행 횟수에 따른 상대도수의 변화 곡선\")\n    def freq_plot():\n        p = TRIALS[input.trial()][\"p\"]\n        hits = draws()\n        n = len(hits)\n        run = np.cumsum(hits) / np.arange(1, n + 1)\n\n        fig, ax = plt.subplots()\n        ax.plot(np.arange(1, n + 1), run, color=ACCENT, linewidth=1.2)\n        ax.axhline(p, color=INK, linewidth=1.0, linestyle=\"--\")\n        ax.set_ylim(0, 1)\n        ax.set_xlabel(\"trials\", color=MUTED)\n        ax.set_ylabel(\"relative freq.\", color=MUTED)\n        tufte(ax)\n        return fig\n\n    @render.ui\n    def readout():\n        sc = TRIALS[input.trial()]\n        hits = draws()\n        n = len(hits)\n        rel = hits.mean()\n        return ui.HTML(\n            f'&lt;p class=\"hs-readline\"&gt;'\n            f\"{n:,}번 시행했을 때 상대도수는 {rel:.4f}, \"\n            f\"수학적 확률은 {sc['label']} = {sc['p']:.4f}이고 \"\n            f\"차이는 {abs(rel - sc['p']):.4f}입니다. \"\n            f\"시행을 늘릴수록 상대도수가 수학적 확률에 가까워지는데, \"\n            f\"이렇게 정해지는 값이 통계적 확률입니다.&lt;/p&gt;\"\n        )\n\napp = App(app_ui, server)\n\n\n앱의 핵심 계산은 이 몇 줄이 전부입니다.\nimport numpy as np\n\np = 1 / 6                                # 주사위 6의 눈의 수학적 확률\nn = 10000\n\nhits = np.random.random(n) &lt; p           # n번 시행 (성공 여부)\nrun = np.cumsum(hits) / np.arange(1, n + 1)  # 시점별 상대도수\n\nprint(run[9], run[99], run[-1], p)       # n이 커질수록 p에 접근"
  },
  {
    "objectID": "pages/02_prob/prob_basic/index.html#스스로-확인",
    "href": "pages/02_prob/prob_basic/index.html#스스로-확인",
    "title": "확률의 뜻",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — n = 10일 때와 n = 10000일 때 ’다시 시행’을 여러 번 눌러 보라. 곡선의 끝값은 어느 쪽이 더 안정적인가?\n\n\n\n\n\n\\(n = 10\\)일 때는 누를 때마다 끝값이 0.1 단위로 크게 출렁이지만, \\(n = 10000\\)일 때는 거의 매번 수학적 확률 바로 근처에서 끝납니다. 상대도수는 시행 횟수가 충분히 커야 믿을 수 있는 값이 됩니다.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — 윷가락처럼 앞뒤 모양이 다른 물체는 수학적 확률을 구하기 어렵다. 이럴 때 확률은 어떻게 정하는가?\n\n\n\n\n\n각 결과의 가능성이 같다고 볼 수 없으므로 경우의 수의 비로는 구할 수 없습니다. 대신 실제로 많이 던져 보고 상대도수가 다가가는 값, 즉 통계적 확률로 정합니다. 의약품의 치료 효과, 야구 타율처럼 현실의 많은 확률이 이 방식으로 정해집니다.\n\n\n\n\n← 이전: 이항정리 다음: 확률의 덧셈정리 →"
  },
  {
    "objectID": "pages/02_prob/counting/index.html",
    "href": "pages/02_prob/counting/index.html",
    "title": "순열과 조합",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/02_prob/counting/index.html#개념",
    "href": "pages/02_prob/counting/index.html#개념",
    "title": "순열과 조합",
    "section": "개념",
    "text": "개념\n서로 다른 \\(n\\)개에서 \\(r\\)개를 순서를 생각하여 뽑아 나열하는 것이 순열, 순서를 생각하지 않고 뽑기만 하는 것이 조합입니다. 순열의 수와 조합의 수는 다음과 같습니다.\n\\[{}_n\\mathrm{P}_r = \\frac{n!}{(n-r)!}, \\qquad\n{}_n\\mathrm{C}_r = \\frac{n!}{r!\\,(n-r)!} = \\frac{{}_n\\mathrm{P}_r}{r!}\\]\n\n\n반에서 반장·부반장을 뽑으면 누가 어느 자리인지 순서가 중요하니 순열 \\({}_n\\mathrm{P}_2\\), 그냥 대표 2명을 뽑으면 순서가 없으니 조합 \\({}_n\\mathrm{C}_2\\)입니다.\n같은 \\(r\\)개를 뽑아도 순서를 매기는 방법이 \\(r!\\)가지이므로, 순열의 수는 조합의 수의 \\(r!\\)배입니다. 즉 \\({}_n\\mathrm{P}_r = {}_n\\mathrm{C}_r \\times r!\\) 이 항상 성립합니다. 아래 앱에서 \\(n\\)과 \\(r\\)을 움직이며 두 수가 어떻게 변하는지, 그리고 이 관계식이 실제 수로 확인되는지 살펴보세요."
  },
  {
    "objectID": "pages/02_prob/counting/index.html#만지며-배우기",
    "href": "pages/02_prob/counting/index.html#만지며-배우기",
    "title": "순열과 조합",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 640\n\nfrom math import comb, factorial\nfrom shiny import App, render, ui\nimport numpy as np\nimport matplotlib\nimport matplotlib.pyplot as plt\n\nmatplotlib.rcParams[\"font.size\"] = 10\n\nACCENT = \"#2563eb\"\nINK = \"#111827\"\nMUTED = \"#6b7280\"\n\ndef tufte(ax):\n    for side in (\"top\", \"right\"):\n        ax.spines[side].set_visible(False)\n    for side in (\"left\", \"bottom\"):\n        ax.spines[side].set_color(\"#9ca3af\")\n    ax.tick_params(colors=MUTED, labelsize=9)\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_slider(\"n\", \"전체 개수 n\", min=1, max=12, value=6, step=1),\n        ui.input_slider(\"r\", \"뽑는 개수 r\", min=0, max=12, value=2, step=1),\n        width=240,\n    ),\n    ui.card(\n        ui.card_header(\"조합의 수 C(n, k) — 파란 막대가 지금 고른 r\"),\n        ui.output_plot(\"bars\", height=\"340px\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @render.plot(alt=\"k에 따른 조합의 수 막대그래프\")\n    def bars():\n        n = input.n()\n        r_eff = min(input.r(), n)\n        ks = np.arange(n + 1)\n        vals = [comb(n, k) for k in ks]\n        colors = [\"#d1d5db\"] * (n + 1)\n        colors[r_eff] = ACCENT\n\n        fig, ax = plt.subplots()\n        ax.bar(ks, vals, color=colors, edgecolor=\"none\")\n        ax.set_xticks(ks)\n        ax.set_xlabel(\"k\", color=MUTED)\n        ax.set_ylabel(\"C(n, k)\", color=MUTED)\n        tufte(ax)\n        return fig\n\n    @render.ui\n    def readout():\n        n, r = input.n(), input.r()\n        r_eff = min(r, n)\n        npr = comb(n, r_eff) * factorial(r_eff)\n        ncr = comb(n, r_eff)\n        note = \"\"\n        if r &gt; n:\n            note = (f\" (r = {r}은 n = {n}보다 클 수 없어 \"\n                    f\"r = {r_eff}로 맞춰 계산했습니다.)\")\n        return ui.HTML(\n            f'&lt;p class=\"hs-readline\"&gt;'\n            f\"{n}P{r_eff} = {npr:,} · {n}C{r_eff} = {ncr:,} 입니다. \"\n            f\"관계식으로 확인하면 {n}P{r_eff} = {n}C{r_eff} × {r_eff}! = \"\n            f\"{ncr:,} × {factorial(r_eff):,} = {npr:,} 이 성립합니다.\"\n            f\"{note}&lt;/p&gt;\"\n        )\n\napp = App(app_ui, server)\n\n\n순열과 조합의 수는 math 모듈로 바로 계산할 수 있습니다.\nfrom math import comb, factorial\n\nn, r = 6, 2\n\nnCr = comb(n, r)                  # 6C2 = 15\nnPr = nCr * factorial(r)          # 6P2 = 15 × 2! = 30\n\nprint(nPr, nCr)                   # 30 15\nprint(nPr == comb(n, r) * factorial(r))  # nPr = nCr × r! 확인"
  },
  {
    "objectID": "pages/02_prob/counting/index.html#스스로-확인",
    "href": "pages/02_prob/counting/index.html#스스로-확인",
    "title": "순열과 조합",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — n을 고정하고 r을 0부터 n까지 움직이면 막대의 높이는 어떤 모양으로 변하는가?\n\n\n\n\n\n가운데(\\(k = n/2\\) 근처)가 가장 높고 양끝(\\(k=0,\\,n\\))이 1인 좌우 대칭 모양입니다. \\({}_n\\mathrm{C}_r = {}_n\\mathrm{C}_{n-r}\\)이기 때문입니다 — \\(r\\)개를 “뽑는” 것과 \\(n-r\\)개를 “남기는” 것은 같은 선택입니다.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — 8명 중 대표 3명을 뽑는 경우의 수와, 회장·부회장·총무를 뽑는 경우의 수는 각각 얼마인가?\n\n\n\n\n\n대표 3명은 순서가 없으니 \\({}_8\\mathrm{C}_3 = 56\\), 회장·부회장·총무는 자리가 구별되니 \\({}_8\\mathrm{P}_3 = 336\\)입니다. \\(336 = 56 \\times 3!\\) — 같은 3명을 세 자리에 배치하는 방법이 \\(3! = 6\\)가지라서 정확히 6배가 됩니다. 앱에서 \\(n=8\\), \\(r=3\\)으로 확인해 보세요.\n\n\n\n\n← 이전: 산점도와 상관관계 다음: 여러 가지 순열 →"
  },
  {
    "objectID": "pages/02_prob/combi_repeat/index.html",
    "href": "pages/02_prob/combi_repeat/index.html",
    "title": "중복조합",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/02_prob/combi_repeat/index.html#개념",
    "href": "pages/02_prob/combi_repeat/index.html#개념",
    "title": "중복조합",
    "section": "개념",
    "text": "개념\n조합은 서로 다른 것에서 순서 없이 뽑는 것이었습니다. 여기서 같은 것을 여러 번 뽑아도 된다고 허용하면 중복조합이 됩니다. 예를 들어 세 종류의 붕어빵(팥·슈크림·피자) 중에서 5개를 살 때, 같은 맛을 몇 개든 골라도 되는 상황이 중복조합입니다.\n서로 다른 \\(n\\)개에서 중복을 허용해 \\(r\\)개를 뽑는 중복조합의 수는\n\\[{}_n\\mathrm{H}_r = {}_{n+r-1}\\mathrm{C}_r\\]\n\n\n왜 \\(n+r-1\\)일까요? “무엇을 몇 개 뽑았는가”는 칸막이 \\(n-1\\)개로 종류를 나누고 공 \\(r\\)개를 늘어놓는 배열과 같습니다. 전체 \\(n+r-1\\)개 자리 중 공이 놓일 \\(r\\)자리를 고르는 문제가 되어 \\({}_{n+r-1}\\mathrm{C}_r\\)입니다.\n핵심은 공과 칸막이(●와 │) 로 바꿔 세는 것입니다. 종류가 \\(n\\)개면 칸막이는 \\(n-1\\)개, 뽑는 개수가 \\(r\\)개면 공은 \\(r\\)개입니다. 이 \\(n+r-1\\)개를 일렬로 놓는 방법 중 공의 자리를 고르면 되므로 \\({}_{n+r-1}\\mathrm{C}_r\\)가지가 됩니다. 아래 앱에서 종류와 개수를 바꾸며 공-칸막이 그림과 그 수가 어떻게 변하는지 보세요."
  },
  {
    "objectID": "pages/02_prob/combi_repeat/index.html#만지며-배우기",
    "href": "pages/02_prob/combi_repeat/index.html#만지며-배우기",
    "title": "중복조합",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 620\n\nfrom math import comb\nfrom shiny import App, render, ui\n\nACCENT = \"#2563eb\"\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_slider(\"n\", \"붕어빵 종류 수 (n)\", min=2, max=6, value=3, step=1),\n        ui.input_slider(\"r\", \"사는 개수 (r)\", min=1, max=8, value=5, step=1),\n        width=250,\n    ),\n    ui.card(\n        ui.card_header(\"공(●)과 칸막이(│) 로 바꿔 세기\"),\n        ui.output_ui(\"diagram\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @render.ui\n    def diagram():\n        n, r = input.n(), input.r()\n        # 한 가지 예시 분배: 첫 종류에 몰아주고 나머지는 한 개씩 흩뿌리기\n        counts = [0] * n\n        for k in range(r):\n            counts[k % n] += 1\n        parts = []\n        for i, c in enumerate(counts):\n            balls = \"\".join(\n                f'&lt;span style=\"color:{ACCENT}; font-size:1.4em;\"&gt;●&lt;/span&gt;'\n                for _ in range(c)\n            )\n            parts.append(balls if c else\n                         '&lt;span style=\"color:#d1d5db;\"&gt;·&lt;/span&gt;')\n        bar = ('&lt;span style=\"color:#9ca3af; font-size:1.4em;'\n               ' margin:0 6px;\"&gt;│&lt;/span&gt;')\n        row = bar.join(parts)\n        labels = \" · \".join(f\"{i+1}번 {c}개\" for i, c in enumerate(counts))\n        return ui.HTML(\n            f'&lt;div style=\"padding:1em 0.3em; letter-spacing:2px;\"&gt;{row}&lt;/div&gt;'\n            f'&lt;div style=\"color:#6b7280; font-size:0.85em;\"&gt;예시 분배: {labels}&lt;/div&gt;'\n        )\n\n    @render.ui\n    def readout():\n        n, r = input.n(), input.r()\n        val = comb(n + r - 1, r)\n        return ui.HTML(\n            '&lt;p class=\"hs-readline\"&gt;'\n            f\"{n}종류에서 중복을 허용해 {r}개를 고르는 방법은 \"\n            f\"nHr = (n+r-1)Cr = {n + r - 1}C{r} = &lt;b&gt;{val:,}가지&lt;/b&gt;입니다. \"\n            f\"공 {r}개와 칸막이 {n - 1}개, 모두 {n + r - 1}개를 늘어놓고 \"\n            f\"그중 공이 놓일 {r}자리를 고르는 문제와 똑같습니다.&lt;/p&gt;\"\n        )\n\napp = App(app_ui, server)\n\n\n중복조합은 조합 함수 comb 로 자리 수만 바꾸면 됩니다.\nfrom math import comb\n\n# 3종류 붕어빵에서 5개 사기 (중복 허용)\nn, r = 3, 5\nprint(comb(n + r - 1, r))            # 7C5 = 21\n\n# 비교: 서로 다른 3개에서 순서 없이 5개는 불가능하지만,\n# 중복을 허용하면 21가지가 나온다."
  },
  {
    "objectID": "pages/02_prob/combi_repeat/index.html#스스로-확인",
    "href": "pages/02_prob/combi_repeat/index.html#스스로-확인",
    "title": "중복조합",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — 세 종류 음료에서 4잔을 고르는 방법은 몇 가지인가? (같은 종류 여러 잔 가능)\n\n\n\n\n\n중복조합 \\({}_3\\mathrm{H}_4 = {}_{3+4-1}\\mathrm{C}_4 = {}_6\\mathrm{C}_4 = 15\\)가지입니다. 공 4개와 칸막이 2개, 모두 6개를 늘어놓고 공의 자리 4개를 고르는 것과 같습니다.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — 중복조합과 중복순열은 무엇이 다른가?\n\n\n\n\n\n둘 다 같은 것을 다시 뽑을 수 있지만, 순서를 따지는지가 다릅니다. 중복순열 \\({}_n\\Pi_r = n^r\\)는 뽑은 순서까지 구별하고, 중복조합 \\({}_n\\mathrm{H}_r\\)는 “무엇을 몇 개” 만 보고 순서는 무시합니다. 그래서 같은 \\(n,\\,r\\)이면 항상 \\({}_n\\mathrm{H}_r \\le {}_n\\Pi_r\\)입니다.\n\n\n\n\n← 이전: 여러 가지 순열 다음: 이항정리 →"
  },
  {
    "objectID": "pages/02_prob/add_rule/index.html",
    "href": "pages/02_prob/add_rule/index.html",
    "title": "확률의 덧셈정리",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/02_prob/add_rule/index.html#개념",
    "href": "pages/02_prob/add_rule/index.html#개념",
    "title": "확률의 덧셈정리",
    "section": "개념",
    "text": "개념\n두 사건 \\(A\\) 또는 \\(B\\)가 일어날 확률은 각각을 더한 뒤, 겹치는 부분을 한 번 빼서 구합니다. 겹치는 \\(A\\cap B\\)를 그냥 더하면 두 번 세어지기 때문입니다. 이것이 확률의 덧셈정리입니다.\n\\[\\mathrm{P}(A\\cup B) = \\mathrm{P}(A) + \\mathrm{P}(B) - \\mathrm{P}(A\\cap B)\\]\n\n\n1부터 20까지 카드에서 “3의 배수 또는 5의 배수”를 세어 봅시다. 3의 배수는 6장, 5의 배수는 4장이지만, 15는 둘 다이므로 한 번만 세야 합니다. 그래서 \\(6 + 4 - 1 = 9\\)장입니다.\n두 사건이 동시에 일어날 수 없으면(배반사건) 겹치는 부분이 없으므로 \\(\\mathrm{P}(A\\cap B)=0\\)이 되어, 덧셈정리는 간단히 다음이 됩니다.\n\\[\\mathrm{P}(A\\cup B) = \\mathrm{P}(A) + \\mathrm{P}(B)\\quad(\\text{배반사건})\\]\n아래 앱에서 두 배수 조건을 바꾸며, 겹치는 카드가 있을 때와 없을 때 합집합의 크기가 어떻게 달라지는지 색으로 확인해 보세요."
  },
  {
    "objectID": "pages/02_prob/add_rule/index.html#만지며-배우기",
    "href": "pages/02_prob/add_rule/index.html#만지며-배우기",
    "title": "확률의 덧셈정리",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 660\n\nfrom fractions import Fraction\nfrom shiny import App, render, ui\n\nN = 20\nA_COLOR = \"#2563eb\"   # A만\nB_COLOR = \"#f59e0b\"   # B만\nBOTH = \"#7c3aed\"      # 교집합\nNONE = \"#e5e7eb\"      # 해당 없음\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_select(\"a\", \"사건 A: 이 수의 배수\",\n                        {str(k): f\"{k}의 배수\" for k in (2, 3, 4, 5)},\n                        selected=\"3\"),\n        ui.input_select(\"b\", \"사건 B: 이 수의 배수\",\n                        {str(k): f\"{k}의 배수\" for k in (2, 3, 4, 5)},\n                        selected=\"5\"),\n        width=250,\n    ),\n    ui.card(\n        ui.card_header(\"1~20 카드 — 파랑=A, 주황=B, 보라=둘 다\"),\n        ui.output_ui(\"grid\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    def sets():\n        a, b = int(input.a()), int(input.b())\n        A = {k for k in range(1, N + 1) if k % a == 0}\n        B = {k for k in range(1, N + 1) if k % b == 0}\n        return a, b, A, B\n\n    @render.ui\n    def grid():\n        a, b, A, B = sets()\n        cells = \"\"\n        for k in range(1, N + 1):\n            inA, inB = k in A, k in B\n            if inA and inB:\n                bg, fg = BOTH, \"white\"\n            elif inA:\n                bg, fg = A_COLOR, \"white\"\n            elif inB:\n                bg, fg = B_COLOR, \"white\"\n            else:\n                bg, fg = NONE, \"#6b7280\"\n            cells += (\n                f'&lt;div style=\"width:38px; height:38px; border-radius:6px;'\n                f' background:{bg}; color:{fg}; display:flex;'\n                f' align-items:center; justify-content:center;'\n                f' font-size:0.9em; font-weight:600;\"&gt;{k}&lt;/div&gt;'\n            )\n        return ui.HTML(\n            '&lt;div style=\"display:flex; flex-wrap:wrap; gap:6px;'\n            f' padding:0.4em 0.2em;\"&gt;{cells}&lt;/div&gt;'\n        )\n\n    @render.ui\n    def readout():\n        a, b, A, B = sets()\n        both = A & B\n        union = A | B\n        pa, pb = Fraction(len(A), N), Fraction(len(B), N)\n        pab = Fraction(len(both), N)\n        pu = Fraction(len(union), N)\n        disjoint = \"겹치는 카드가 없어 배반사건입니다. \" if not both else \"\"\n        return ui.HTML(\n            '&lt;p class=\"hs-readline\"&gt;'\n            f\"P(A) = {len(A)}/{N}, P(B) = {len(B)}/{N}, \"\n            f\"P(A∩B) = {len(both)}/{N} 이므로&lt;br&gt;\"\n            f\"P(A∪B) = {pa} + {pb} − {pab} = &lt;b&gt;{pu}&lt;/b&gt; \"\n            f\"(카드 {len(union)}장). {disjoint}\"\n            \"겹치는 부분을 빼지 않으면 그만큼 확률이 부풀려집니다.&lt;/p&gt;\"\n        )\n\napp = App(app_ui, server)\n\n\n집합 연산으로 덧셈정리를 그대로 확인할 수 있습니다.\nfrom fractions import Fraction\n\nN = 20\nA = {k for k in range(1, N + 1) if k % 3 == 0}   # 3의 배수\nB = {k for k in range(1, N + 1) if k % 5 == 0}   # 5의 배수\n\nunion = Fraction(len(A | B), N)\nby_rule = Fraction(len(A), N) + Fraction(len(B), N) - Fraction(len(A & B), N)\nprint(union, by_rule)                # 9/20 9/20 — 일치"
  },
  {
    "objectID": "pages/02_prob/add_rule/index.html#스스로-확인",
    "href": "pages/02_prob/add_rule/index.html#스스로-확인",
    "title": "확률의 덧셈정리",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — 1~20 카드에서 ’2의 배수 또는 3의 배수’일 확률은?\n\n\n\n\n\n2의 배수는 10장, 3의 배수는 6장, 둘 다인 6의 배수는 3장(6·12·18)입니다. 덧셈정리로 \\(\\dfrac{10}{20} + \\dfrac{6}{20} - \\dfrac{3}{20} = \\dfrac{13}{20}\\)입니다. 겹치는 6의 배수를 빼 주는 것이 핵심입니다.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — 언제 P(A∪B)=P(A)+P(B) 로 그냥 더해도 되는가?\n\n\n\n\n\n두 사건이 동시에 일어날 수 없을 때(배반사건, \\(A\\cap B=\\varnothing\\))입니다. 예를 들어 한 장을 뽑아 “3의 배수”와 “3으로 나눈 나머지가 1”은 동시에 성립할 수 없으므로, 두 확률을 그냥 더하면 됩니다. 겹침이 조금이라도 있으면 반드시 \\(\\mathrm{P}(A\\cap B)\\)를 빼야 합니다.\n\n\n\n\n← 이전: 확률의 뜻 다음: 조건부확률과 독립 →"
  },
  {
    "objectID": "pages/01_data/spread/index.html",
    "href": "pages/01_data/spread/index.html",
    "title": "산포도와 상자그림",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/01_data/spread/index.html#개념",
    "href": "pages/01_data/spread/index.html#개념",
    "title": "산포도와 상자그림",
    "section": "개념",
    "text": "개념\n대푯값이 같아도 자료가 흩어진 정도는 다를 수 있습니다. 흩어진 정도를 재는 값이 산포도입니다. 각 값과 평균의 차이(편차)를 제곱해 평균 낸 것이 분산, 그 양의 제곱근이 표준편차입니다.\n\\[\\sigma^2 = \\frac{(x_1-m)^2 + (x_2-m)^2 + \\cdots + (x_n-m)^2}{n}, \\qquad \\sigma = \\sqrt{\\sigma^2}\\]\n\n\n두 반의 수학 평균이 똑같이 70점이어도, 한 반은 모두 70점 근처이고 다른 반은 40점과 100점이 뒤섞여 있을 수 있습니다. 평균만 보면 이 차이가 전혀 보이지 않습니다.\n자료를 크기순으로 4등분하는 값이 사분위수(\\(Q_1, Q_2, Q_3\\))이고, 이를 그림으로 요약한 것이 상자그림입니다. 상자의 아래·위 변이 \\(Q_1\\)과 \\(Q_3\\), 가운데 선이 중앙값 \\(Q_2\\)이며, 상자의 높이 \\(Q_3 - Q_1\\)이 사분위범위(IQR)입니다. 상자가 길수록, 수염이 넓게 퍼질수록 자료의 흩어짐이 큽니다. 아래 앱에서 평균이 같은 두 그룹의 표준편차를 다르게 바꿔 보세요."
  },
  {
    "objectID": "pages/01_data/spread/index.html#만지며-배우기",
    "href": "pages/01_data/spread/index.html#만지며-배우기",
    "title": "산포도와 상자그림",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 640\n\nfrom shiny import App, reactive, render, ui\nimport numpy as np\nimport matplotlib\nimport matplotlib.pyplot as plt\n\nmatplotlib.rcParams[\"font.size\"] = 10\n\nACCENT = \"#2563eb\"\nINK = \"#111827\"\nMUTED = \"#6b7280\"\n\ndef tufte(ax):\n    for side in (\"top\", \"right\"):\n        ax.spines[side].set_visible(False)\n    for side in (\"left\", \"bottom\"):\n        ax.spines[side].set_color(\"#9ca3af\")\n    ax.tick_params(colors=MUTED, labelsize=9)\n\nN = 100  # 각 그룹의 자료 수\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_slider(\"sa\", \"그룹 A의 표준편차 σ_A\", min=2, max=20, value=5, step=1),\n        ui.input_slider(\"sb\", \"그룹 B의 표준편차 σ_B\", min=2, max=20, value=12, step=1),\n        ui.input_action_button(\"regen\", \"다시 생성\"),\n        width=240,\n    ),\n    ui.card(\n        ui.card_header(\"상자그림 — 두 그룹 모두 평균 70점 근처입니다\"),\n        ui.output_plot(\"boxes\", height=\"340px\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @reactive.calc\n    def groups():\n        input.regen()  # 버튼을 누르면 새로 생성\n        a = np.random.normal(70, input.sa(), N)\n        b = np.random.normal(70, input.sb(), N)\n        return a, b\n\n    @render.plot(alt=\"두 그룹의 상자그림과 개별 점\")\n    def boxes():\n        a, b = groups()\n\n        fig, ax = plt.subplots()\n        for pos, vals in ((1, a), (2, b)):\n            jitter = pos + np.random.uniform(-0.13, 0.13, N)\n            ax.scatter(jitter, vals, s=14, color=\"gray\", alpha=0.25, zorder=1)\n        bp = ax.boxplot([a, b], widths=0.45, zorder=2)\n        for med in bp[\"medians\"]:\n            med.set_color(ACCENT)\n            med.set_linewidth(1.6)\n        ax.set_xticks([1, 2])\n        ax.set_xticklabels([\"A\", \"B\"])\n        ax.set_ylabel(\"score\", color=MUTED)\n        tufte(ax)\n        return fig\n\n    @render.ui\n    def readout():\n        a, b = groups()\n        sa, sb = a.std(), b.std()\n        iqr_a = np.percentile(a, 75) - np.percentile(a, 25)\n        iqr_b = np.percentile(b, 75) - np.percentile(b, 25)\n        wide = \"B\" if sb &gt; sa else \"A\"\n        return ui.HTML(\n            '&lt;p class=\"hs-readline\"&gt;'\n            f\"A: 평균 {a.mean():.1f} · 표준편차 {sa:.2f} · 분산 {sa**2:.1f} · IQR {iqr_a:.1f} — \"\n            f\"B: 평균 {b.mean():.1f} · 표준편차 {sb:.2f} · 분산 {sb**2:.1f} · IQR {iqr_b:.1f}. \"\n            f\"두 그룹의 평균은 거의 같아도 {wide} 그룹의 상자와 수염이 더 길어, \"\n            \"흩어짐(산포)이 더 크다는 것을 한눈에 볼 수 있습니다.&lt;/p&gt;\"\n        )\n\napp = App(app_ui, server)\n\n\n표준편차·분산·사분위수는 numpy 함수 하나씩입니다.\nimport numpy as np\n\na = np.random.normal(70, 5, 100)    # 평균 70, 표준편차 5인 그룹\nb = np.random.normal(70, 12, 100)   # 평균은 같고 더 넓게 퍼진 그룹\n\nprint(a.std(), b.std())             # 표준편차 σ\nprint(a.var(), b.var())             # 분산 σ²\n\nq1, q3 = np.percentile(b, [25, 75]) # 사분위수 Q1, Q3\nprint(q3 - q1)                      # 사분위범위 IQR"
  },
  {
    "objectID": "pages/01_data/spread/index.html#스스로-확인",
    "href": "pages/01_data/spread/index.html#스스로-확인",
    "title": "산포도와 상자그림",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — σ_A = σ_B로 맞추고 ’다시 생성’을 여러 번 눌러 보라. 두 상자그림은 완전히 똑같아지는가?\n\n\n\n\n\n아닙니다. 같은 분포에서 뽑아도 매번 다른 100명이 뽑히므로 상자그림은 조금씩 다르게 나옵니다. 다만 상자의 위치와 길이는 비슷한 범위 안에서 움직입니다. 이 “표본마다 달라지는 정도”는 04단원 통계적 추정의 출발점입니다.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — 표준편차가 커지면 상자그림의 어느 부분이 어떻게 변하는가?\n\n\n\n\n\n상자(\\(Q_1\\)~\\(Q_3\\))가 세로로 길어지고 수염도 넓게 퍼집니다. 즉 IQR과 전체 범위가 함께 커집니다. 반대로 중앙값(상자 속 선)의 위치는 평균이 70으로 같으므로 거의 움직이지 않습니다.\n\n\n\n\n← 이전: 대푯값 다음: 산점도와 상관관계 →"
  },
  {
    "objectID": "pages/01_data/freq_table/index.html",
    "href": "pages/01_data/freq_table/index.html",
    "title": "도수분포표와 히스토그램",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/01_data/freq_table/index.html#개념",
    "href": "pages/01_data/freq_table/index.html#개념",
    "title": "도수분포표와 히스토그램",
    "section": "개념",
    "text": "개념\n자료를 일정한 구간(계급)으로 나누고, 각 계급에 속하는 자료의 개수(도수)를 센 표가 도수분포표입니다. 각 계급의 도수를 전체 자료 수로 나눈 값이 상대도수이며, 상대도수를 모두 더하면 항상 1이 됩니다.\n\\[\\text{상대도수} = \\frac{\\text{그 계급의 도수}}{\\text{전체 도수}}\\]\n\n\n“우리 반 학생들의 하루 스마트폰 사용 시간”처럼 학급 설문으로 모은 자료가 바로 이런 표와 그래프의 좋은 재료입니다.\n도수분포표를 그래프로 옮긴 것이 히스토그램입니다. 가로축은 계급, 세로축은 도수(또는 상대도수)이고, 계급이 연속이므로 막대 사이를 붙여 그립니다. 계급의 크기를 어떻게 잡느냐에 따라 같은 자료도 전혀 다르게 보일 수 있습니다. 아래 앱에서 직접 확인해 보세요."
  },
  {
    "objectID": "pages/01_data/freq_table/index.html#만지며-배우기",
    "href": "pages/01_data/freq_table/index.html#만지며-배우기",
    "title": "도수분포표와 히스토그램",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 700\n\nimport math\nfrom shiny import App, render, ui\nimport numpy as np\nimport matplotlib\nimport matplotlib.pyplot as plt\n\nmatplotlib.rcParams[\"font.size\"] = 10\n\nACCENT = \"#2563eb\"\nINK = \"#111827\"\nMUTED = \"#6b7280\"\n\ndef tufte(ax):\n    for side in (\"top\", \"right\"):\n        ax.spines[side].set_visible(False)\n    for side in (\"left\", \"bottom\"):\n        ax.spines[side].set_color(\"#9ca3af\")\n    ax.tick_params(colors=MUTED, labelsize=9)\n\n# 학생 40명의 하루 스마트폰 사용 시간(분)\nDATA = [\n    42, 55, 63, 71, 78, 84, 90, 96,\n    101, 107, 112, 118, 121, 125, 128, 132,\n    135, 139, 142, 146, 149, 153, 157, 161,\n    164, 168, 173, 179, 185, 192, 200, 209,\n    218, 228, 238, 249, 258, 267, 276, 288,\n]\nLO = 40  # 첫 계급의 시작값\n\ndef bin_edges(width):\n    n_bins = int(math.ceil((max(DATA) - LO) / width))\n    return [LO + width * i for i in range(n_bins + 1)]\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_slider(\"width\", \"계급의 크기(분)\", min=10, max=60, value=30, step=10),\n        ui.input_checkbox(\"relative\", \"상대도수로 보기\", False),\n        width=240,\n    ),\n    ui.card(\n        ui.card_header(\"히스토그램 — 계급의 크기에 따라 모양이 달라집니다\"),\n        ui.output_plot(\"hist\", height=\"340px\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @render.plot(alt=\"스마트폰 사용 시간 히스토그램\")\n    def hist():\n        edges = bin_edges(input.width())\n        n = len(DATA)\n        weights = [1 / n] * n if input.relative() else None\n\n        fig, ax = plt.subplots()\n        ax.hist(DATA, bins=edges, weights=weights,\n                color=ACCENT, alpha=0.85, edgecolor=INK, linewidth=0.8)\n        ax.set_xlabel(\"x (min)\", color=MUTED)\n        ax.set_ylabel(\"relative freq.\" if input.relative() else \"freq.\",\n                      color=MUTED)\n        ax.set_xticks(edges)\n        tufte(ax)\n        return fig\n\n    @render.ui\n    def readout():\n        edges = bin_edges(input.width())\n        counts, _ = np.histogram(DATA, bins=edges)\n        n = len(DATA)\n\n        td = 'style=\"border:1px solid #d1d5db; padding:2px 10px; text-align:center;\"'\n        rows = \"\"\n        for i, c in enumerate(counts):\n            rows += (f\"&lt;tr&gt;&lt;td {td}&gt;{edges[i]} 이상 ~ {edges[i+1]} 미만&lt;/td&gt;\"\n                     f\"&lt;td {td}&gt;{c}&lt;/td&gt;\"\n                     f\"&lt;td {td}&gt;{c / n:.3f}&lt;/td&gt;&lt;/tr&gt;\")\n        rows += (f\"&lt;tr&gt;&lt;td {td}&gt;&lt;b&gt;합계&lt;/b&gt;&lt;/td&gt;\"\n                 f\"&lt;td {td}&gt;&lt;b&gt;{n}&lt;/b&gt;&lt;/td&gt;\"\n                 f\"&lt;td {td}&gt;&lt;b&gt;1.000&lt;/b&gt;&lt;/td&gt;&lt;/tr&gt;\")\n        table = (\n            '&lt;table style=\"border-collapse:collapse; margin:0 auto; font-size:0.9em;\"&gt;'\n            f\"&lt;tr&gt;&lt;th {td}&gt;계급(분)&lt;/th&gt;&lt;th {td}&gt;도수(명)&lt;/th&gt;&lt;th {td}&gt;상대도수&lt;/th&gt;&lt;/tr&gt;\"\n            f\"{rows}&lt;/table&gt;\"\n        )\n        return ui.HTML(\n            table\n            + '&lt;p class=\"hs-readline\"&gt;'\n            f\"계급의 크기가 {input.width()}분일 때 계급은 모두 {len(counts)}개입니다. \"\n            \"계급의 크기를 키우면 표와 그래프는 단순해지지만, \"\n            \"분포의 세부 모양은 점점 뭉개집니다.&lt;/p&gt;\"\n        )\n\napp = App(app_ui, server)\n\n\n도수분포표의 핵심은 np.histogram 한 줄입니다.\nimport numpy as np\n\ndata = [42, 55, 63, 71, 78, 84, 90, 96, 101, 107]  # 사용 시간(분)\n\nedges = [40, 70, 100, 130]                 # 계급의 경계 (크기 30)\ncounts, _ = np.histogram(data, bins=edges) # 각 계급의 도수\n\nfor i, c in enumerate(counts):\n    print(f\"{edges[i]} 이상 ~ {edges[i+1]} 미만: 도수 {c}, \"\n          f\"상대도수 {c / len(data):.2f}\")"
  },
  {
    "objectID": "pages/01_data/freq_table/index.html#스스로-확인",
    "href": "pages/01_data/freq_table/index.html#스스로-확인",
    "title": "도수분포표와 히스토그램",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — 계급의 크기를 10분에서 60분까지 바꾸면 히스토그램은 어떻게 변하는가?\n\n\n\n\n\n계급이 좁으면 막대가 많아져 자료의 세부가 드러나지만 들쭉날쭉해지고, 계급이 넓으면 매끈해지는 대신 분포의 모양(어디에 몰려 있는지)이 뭉개집니다. 자료의 특징이 가장 잘 보이는 “적당한” 계급의 크기를 직접 찾아보세요.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — 상대도수로 바꾸면 막대의 높이는 변하는데 모양은 왜 그대로인가?\n\n\n\n\n\n상대도수는 모든 도수를 같은 수(전체 도수 40)로 나눈 값이라, 세로축의 눈금만 바뀌고 막대들 사이의 비율은 그대로이기 때문입니다. 상대도수는 전체 자료 수가 다른 두 집단을 비교할 때 특히 유용합니다.\n\n\n\n\n← 이전: 정보의 구조화 다음: 대푯값 →"
  },
  {
    "objectID": "pages/01_data/center/index.html",
    "href": "pages/01_data/center/index.html",
    "title": "대푯값",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/01_data/center/index.html#개념",
    "href": "pages/01_data/center/index.html#개념",
    "title": "대푯값",
    "section": "개념",
    "text": "개념\n자료 전체를 하나의 수로 요약한 값이 대푯값입니다. 가장 많이 쓰는 세 가지는 모든 값을 더해 개수로 나눈 평균, 크기순으로 늘어놓았을 때 한가운데 값인 중앙값, 가장 자주 나타나는 값인 최빈값입니다.\n\\[\\text{평균} = \\frac{x_1 + x_2 + \\cdots + x_n}{n}\\]\n\n\n“직장인 평균 연봉”이 체감보다 높게 느껴지는 이유가 여기 있습니다. 소수의 고액 연봉이 평균을 끌어올리기 때문에, 뉴스에서는 중앙값을 함께 보여 주기도 합니다.\n세 값은 성격이 다릅니다. 평균은 모든 값을 반영하는 대신 극단적으로 크거나 작은 값(이상값)에 민감하게 끌려가고, 중앙값과 최빈값은 이상값의 영향을 거의 받지 않습니다. 아래 앱에서 전학 온 친구 한 명의 독서 권수를 극단적으로 키워 보며 세 대푯값이 어떻게 움직이는지 비교해 보세요."
  },
  {
    "objectID": "pages/01_data/center/index.html#만지며-배우기",
    "href": "pages/01_data/center/index.html#만지며-배우기",
    "title": "대푯값",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 640\n\nfrom shiny import App, render, ui\nimport numpy as np\nimport matplotlib\nimport matplotlib.pyplot as plt\n\nmatplotlib.rcParams[\"font.size\"] = 10\n\nACCENT = \"#2563eb\"\nINK = \"#111827\"\nMUTED = \"#6b7280\"\n\ndef tufte(ax):\n    for side in (\"top\", \"right\"):\n        ax.spines[side].set_visible(False)\n    for side in (\"left\", \"bottom\"):\n        ax.spines[side].set_color(\"#9ca3af\")\n    ax.tick_params(colors=MUTED, labelsize=9)\n\n# 학생 15명의 한 달 독서 권수\nREADS = [0, 1, 2, 2, 3, 3, 3, 3, 3, 4, 4, 5, 5, 6, 8]\n\ndef stats(data):\n    arr = np.array(data)\n    mean = arr.mean()\n    median = np.median(arr)\n    vals, counts = np.unique(arr, return_counts=True)\n    mode = vals[np.argmax(counts)]\n    return mean, median, mode\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_slider(\"newbie\", \"전학 온 친구의 독서 권수\", min=0, max=100, value=3, step=1),\n        width=240,\n    ),\n    ui.card(\n        ui.card_header(\"점그래프 — 세로선이 평균·중앙값·최빈값입니다\"),\n        ui.output_plot(\"dots\", height=\"340px\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @render.plot(alt=\"독서 권수 점그래프와 세 대푯값\")\n    def dots():\n        data = READS + [input.newbie()]\n        mean, median, mode = stats(data)\n\n        fig, ax = plt.subplots()\n        seen = {}\n        for v in sorted(data):\n            seen[v] = seen.get(v, 0) + 1\n            ax.scatter(v, seen[v], s=60, color=ACCENT, alpha=0.8, zorder=3)\n\n        ax.axvline(mean, color=ACCENT, linewidth=1.6,\n                   linestyle=\"-\", label=\"mean\")\n        ax.axvline(median, color=INK, linewidth=1.4,\n                   linestyle=\"--\", label=\"median\")\n        ax.axvline(mode, color=MUTED, linewidth=1.4,\n                   linestyle=\":\", label=\"mode\")\n\n        ax.set_xlim(-1, max(10, input.newbie() + 2))\n        ax.set_ylim(0, max(seen.values()) + 1.5)\n        ax.set_yticks([])\n        ax.set_xlabel(\"x (books)\", color=MUTED)\n        ax.legend(frameon=False, fontsize=9, loc=\"upper right\")\n        tufte(ax)\n        return fig\n\n    @render.ui\n    def readout():\n        data = READS + [input.newbie()]\n        mean, median, mode = stats(data)\n        return ui.HTML(\n            '&lt;p class=\"hs-readline\"&gt;'\n            f\"평균 = {mean:.2f}권 · 중앙값 = {median:.1f}권 · 최빈값 = {mode}권. \"\n            \"슬라이더로 전학 온 친구의 독서 권수를 크게 키우면 평균만 크게 끌려가고, \"\n            \"중앙값과 최빈값은 거의 움직이지 않습니다. \"\n            \"이상값에 대한 민감도가 세 대푯값의 가장 큰 차이입니다.&lt;/p&gt;\"\n        )\n\napp = App(app_ui, server)\n\n\n세 대푯값은 numpy로 이렇게 구합니다.\nimport numpy as np\n\nreads = [0, 1, 2, 2, 3, 3, 3, 3, 3, 4, 4, 5, 5, 6, 8]\narr = np.array(reads + [100])            # 극단값 하나 추가\n\nprint(arr.mean())                        # 평균 — 크게 끌려감\nprint(np.median(arr))                    # 중앙값 — 거의 그대로\n\nvals, counts = np.unique(arr, return_counts=True)\nprint(vals[np.argmax(counts)])           # 최빈값 — 그대로 3"
  },
  {
    "objectID": "pages/01_data/center/index.html#스스로-확인",
    "href": "pages/01_data/center/index.html#스스로-확인",
    "title": "대푯값",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — 슬라이더를 3에서 100으로 올리면 세 대푯값은 각각 얼마나 변하는가?\n\n\n\n\n\n평균은 자료 16개의 합이 97만큼 늘어 약 6권 넘게 커지지만, 중앙값은 크기순 배열의 한가운데 위치가 그대로라 거의 변하지 않고, 최빈값 3권은 전혀 변하지 않습니다. 값 하나가 평균을 얼마나 좌우할 수 있는지 눈으로 확인해 보세요.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — 어떤 자료에는 평균보다 중앙값이 더 좋은 대푯값인가?\n\n\n\n\n\n연봉·집값·SNS 팔로워 수처럼 한쪽으로 길게 꼬리가 늘어진 자료입니다. 극단적으로 큰 값 몇 개가 평균을 끌어올려 “보통”과 멀어지게 하므로, 이런 자료의 대푯값으로는 중앙값이 더 적절합니다.\n\n\n\n\n← 이전: 도수분포표와 히스토그램 다음: 산포도와 상자그림 →"
  },
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "빛스탯 HS",
    "section": "",
    "text": "BitStat HS\n  고교 통계, 만지며 배운다\n  슬라이더를 움직이면 그래프가 답합니다. 교과서 순서 그대로,\n  설치도 계정도 없이 브라우저에서 — 파이썬이 브라우저 안에서 직접 돌아갑니다.\n  \n    Pyodide 서버리스\n    「확률과 통계」·「통계와 사회」 정렬\n    실생활 데이터\n  \n  정규분포 체험 시작 →\n  교육과정 지도 보기\n\n\n\n  \n  25개 모듈 전부 라이브 — 5개 단원 공개\n\n\n\n  \n    ① 만지기\n    슬라이더·버튼으로 모수를 바꾸면 그래프가 즉시 반응합니다.\n    (Shiny for Python × shinylive 앱)\n  \n  \n    ② 열어보기\n    같은 계산의 파이썬 코드를 탭에서 확인합니다.\n    앱 뒤에 숨은 수식이 몇 줄의 코드인지 눈으로 봅니다.\n  \n  \n    ③ 탐구하기\n    예시 데이터로 수행평가·탐구 프로젝트를 진행합니다.\n    (05 통계와 사회 — 데이터 탐색기 · 탐구 안내)\n  \n\n\n\n  교육과정 지도\n  \n    \n      01\n      자료와 그래프\n      중등 복습 · 진단\n      \n        자료와 정보\n        정보의 구조화\n        도수분포표와 히스토그램\n        대푯값\n        산포도와 상자그림\n        산점도와 상관관계\n      \n    \n    \n      02\n      경우의 수와 확률\n      확률과 통계\n      \n        순열과 조합\n        여러 가지 순열\n        중복조합\n        이항정리\n        확률의 뜻\n        확률의 덧셈정리\n        조건부확률과 독립\n      \n    \n    \n      03\n      확률분포\n      확률과 통계\n      \n        확률변수와 기댓값\n        이항분포\n        연속확률변수와 확률분포\n        정규분포와 표준화\n      \n    \n    \n      04\n      통계적 추정\n      확률과 통계\n      \n        표본추출\n        표본평균의 분포\n        모평균의 신뢰구간\n        모비율의 추정\n      \n    \n    \n      05\n      통계와 사회\n      실생활 데이터 · 수행평가\n      \n        데이터 탐색기\n        통계 그래프 만들기\n        여론조사 읽기\n        통계적 탐구 프로젝트\n      \n    \n  \n\n\n\n첫 방문 시 앱이 뜨기까지 수십 초가 걸릴 수 있습니다(브라우저가 파이썬 실행 환경을 내려받는 시간 —\n이후에는 캐시되어 빠릅니다). 자매 사이트:\n빛스탯2 포털 ·\n제작: 한국 R 사용자회"
  },
  {
    "objectID": "pages/01_data/data_info/index.html",
    "href": "pages/01_data/data_info/index.html",
    "title": "자료와 정보",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/01_data/data_info/index.html#개념",
    "href": "pages/01_data/data_info/index.html#개념",
    "title": "자료와 정보",
    "section": "개념",
    "text": "개념\n관찰이나 측정으로 얻은 가공 전의 사실이 자료(data) 입니다. 숫자·문자·그림·소리처럼 그 자체로는 아직 의미가 잘 드러나지 않습니다. 이 자료를 문제 해결에 도움이 되도록 정리·가공한 것이 정보(information) 이고, 정보가 쌓여 판단의 근거가 되면 지식이 됩니다.\n\\[\\text{자료(data)} \\;\\longrightarrow\\; \\text{정보(information)} \\;\\longrightarrow\\; \\text{지식(knowledge)}\\]\n\n\n중등 「정보」 교과서는 자료를 “컴퓨터가 처리할 수 있는 문자·숫자·소리·그림 형태의 것”, 정보를 “관찰이나 측정을 통해 수집한 자료를 실제 문제에 도움이 될 수 있도록 정리한 지식” 으로 구분합니다. 통계는 바로 이 자료 → 정보 변환을 다루는 과목입니다.\n자료는 값의 성격에 따라 나뉩니다. 인구수·키·가격처럼 크기를 재는 수치형(양적) 자료와, 행정구역·성별·선호도처럼 종류를 나누는 범주형(질적) 자료입니다. 어떤 유형이냐에 따라 쓸 수 있는 그래프와 대푯값이 달라지므로, 분석에 앞서 자료의 유형부터 확인해야 합니다.\n문제를 데이터로 해결할 때는 대체로 네 단계를 거칩니다 — ① 문제 분석(무엇이 필요한가) → ② 자료 수집(인터넷·설문으로 모으기) → ③ 분류·관리(스프레드시트로 정렬·선택·분석) → ④ 공유(정리한 결과를 함께 보기). 아래 앱은 이 가운데 ③의 핵심인 정렬을 다룹니다. 같은 표라도 정렬하고 상위 몇 개만 추리면, 뒤섞인 자료가 곧바로 읽히는 정보로 바뀝니다."
  },
  {
    "objectID": "pages/01_data/data_info/index.html#만지며-배우기",
    "href": "pages/01_data/data_info/index.html#만지며-배우기",
    "title": "자료와 정보",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 680\n\nfrom shiny import App, render, ui\n\nACCENT = \"#2563eb\"\n\n# 우리나라 시도별 인구수 — 중등 「정보」 교과서 예시 표\n# (matplotlib 대신 HTML 막대를 쓰는 이유: Pyodide에는 한글 폰트가 없어\n#  차트 축 라벨의 한글이 깨진다. HTML 막대는 브라우저 폰트로 그려 정상 표시된다.)\nPOP = [\n    (\"서울\", 10022181), (\"부산\", 3513777), (\"대구\", 2487829),\n    (\"인천\", 2925815), (\"광주\", 1472199), (\"대전\", 1518775),\n    (\"울산\", 1173534), (\"세종\", 210884), (\"경기\", 12522606),\n    (\"강원\", 1549507), (\"충북\", 1583952), (\"충남\", 2077649),\n    (\"전북\", 1869711), (\"전남\", 1908996), (\"경북\", 2702826),\n    (\"경남\", 3364702), (\"제주\", 624395),\n]\nPOP_MAX = max(p for _, p in POP)\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_select(\n            \"order\", \"정렬 기준\",\n            {\"desc\": \"인구 많은 순\", \"asc\": \"인구 적은 순\", \"name\": \"가나다 순\"},\n            selected=\"desc\",\n        ),\n        ui.input_slider(\"topn\", \"상위 몇 개 시도만 보기\", min=3, max=17, value=8, step=1),\n        width=250,\n    ),\n    ui.card(\n        ui.card_header(\"시도별 인구수 — 정렬하면 뒤섞인 자료가 정보가 됩니다\"),\n        ui.output_ui(\"bars\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    def arranged():\n        data = list(POP)\n        if input.order() == \"desc\":\n            data.sort(key=lambda r: r[1], reverse=True)\n        elif input.order() == \"asc\":\n            data.sort(key=lambda r: r[1])\n        else:\n            data.sort(key=lambda r: r[0])\n        # 가나다 순일 때는 전체를, 크기순일 때는 상위 N개를 보여 준다\n        if input.order() == \"name\":\n            return data\n        return data[: input.topn()]\n\n    @render.ui\n    def bars():\n        rows = \"\"\n        for name, p in arranged():\n            w = p / POP_MAX * 100\n            rows += (\n                '&lt;div style=\"display:flex; align-items:center; margin:5px 0;\"&gt;'\n                f'&lt;div style=\"width:44px; text-align:right; padding-right:10px;'\n                f' font-size:0.9em; flex:none;\"&gt;{name}&lt;/div&gt;'\n                f'&lt;div style=\"height:19px; width:{w:.1f}%; background:{ACCENT};'\n                f' border-radius:3px; flex:none;\"&gt;&lt;/div&gt;'\n                f'&lt;div style=\"padding-left:8px; font-size:0.82em; color:#6b7280;'\n                f' flex:none;\"&gt;{p/10000:,.0f}만&lt;/div&gt;&lt;/div&gt;'\n            )\n        return ui.HTML(f'&lt;div style=\"padding:0.4em 0.2em;\"&gt;{rows}&lt;/div&gt;')\n\n    @render.ui\n    def readout():\n        data = arranged()\n        top = max(POP, key=lambda r: r[1])\n        bot = min(POP, key=lambda r: r[1])\n        total = sum(r[1] for r in POP)\n        return ui.HTML(\n            '&lt;p class=\"hs-readline\"&gt;'\n            f\"17개 시도 인구를 모두 더하면 약 {total/10000:,.0f}만 명입니다. \"\n            f\"가장 많은 곳은 {top[0]}({top[1]:,}명), 가장 적은 곳은 {bot[0]}({bot[1]:,}명)로 \"\n            f\"약 {top[1]/bot[1]:.0f}배 차이가 납니다. \"\n            \"정렬 기준과 개수를 바꿔 보세요. 표의 숫자는 그대로지만, \"\n            \"무엇을 위로 올리고 몇 개만 남기느냐에 따라 전혀 다른 사실이 눈에 들어옵니다. \"\n            \"이것이 자료를 정보로 바꾸는 가장 기본적인 가공입니다.&lt;/p&gt;\"\n        )\n\napp = App(app_ui, server)\n\n\n정렬과 상위 N개 추리기는 sorted 한 줄이면 됩니다.\npop = [\n    (\"서울\", 10022181), (\"경기\", 12522606), (\"부산\", 3513777),\n    (\"세종\", 210884), (\"제주\", 624395),\n]\n\n# 인구 많은 순으로 정렬 (뒤섞인 자료 → 읽히는 정보)\nranked = sorted(pop, key=lambda r: r[1], reverse=True)\n\nfor name, n in ranked[:3]:          # 상위 3개만\n    print(f\"{name}: {n:,}명\")"
  },
  {
    "objectID": "pages/01_data/data_info/index.html#스스로-확인",
    "href": "pages/01_data/data_info/index.html#스스로-확인",
    "title": "자료와 정보",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — ’서울 10,022,181’은 자료인가 정보인가?\n\n\n\n\n\n숫자 하나만 놓고 보면 아직 가공 전의 자료입니다. 이 값을 17개 시도와 함께 인구 많은 순으로 정렬해 “서울이 두 번째로 인구가 많다”처럼 문제 해결에 쓸 수 있게 정리하면 비로소 정보가 됩니다. 같은 값도 정리·해석의 목적에 따라 자료가 되기도, 정보가 되기도 합니다.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — 행정구역과 인구수는 각각 어떤 유형의 자료인가?\n\n\n\n\n\n행정구역(서울·부산…)은 종류를 나누는 범주형(질적) 자료, 인구수는 크기를 재는 수치형(양적) 자료입니다. 범주형은 막대그래프·원그래프로, 수치형은 히스토그램· 상자그림으로 요약하는 것이 자연스럽습니다. 다음 모듈부터 이 그래프들을 하나씩 다룹니다.\n\n\n\n\n← 홈 다음: 정보의 구조화 →"
  },
  {
    "objectID": "pages/01_data/scatter/index.html",
    "href": "pages/01_data/scatter/index.html",
    "title": "산점도와 상관관계",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/01_data/scatter/index.html#개념",
    "href": "pages/01_data/scatter/index.html#개념",
    "title": "산점도와 상관관계",
    "section": "개념",
    "text": "개념\n키와 몸무게처럼 짝을 이룬 두 변량을 좌표평면 위의 점으로 나타낸 그래프가 산점도입니다. 한 변량이 커질 때 다른 변량도 커지는 경향이 있으면 양의 상관관계, 반대로 작아지는 경향이 있으면 음의 상관관계가 있다고 합니다.\n\\[-1 \\;\\le\\; r \\;\\le\\; 1\\]\n\n\n상관관계가 있다고 해서 인과관계가 있는 것은 아닙니다. 아이스크림 판매량과 물놀이 사고는 함께 늘지만, 원인은 둘 다 “더운 날씨”입니다.\n상관관계의 방향과 강도를 하나의 수로 나타낸 것이 상관계수 \\(r\\)입니다. \\(r\\)이 1에 가까울수록 점들이 오른쪽 위로 향하는 직선에 가깝게 모이고, \\(-1\\)에 가까울수록 오른쪽 아래로 향하는 직선에 모이며, 0 근처이면 뚜렷한 직선 경향이 없습니다. 아래 앱에서 \\(r\\)을 움직여 산점도의 모양이 어떻게 변하는지 관찰해 보세요."
  },
  {
    "objectID": "pages/01_data/scatter/index.html#만지며-배우기",
    "href": "pages/01_data/scatter/index.html#만지며-배우기",
    "title": "산점도와 상관관계",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 640\n\nfrom math import sqrt\nfrom shiny import App, reactive, render, ui\nimport numpy as np\nimport matplotlib\nimport matplotlib.pyplot as plt\n\nmatplotlib.rcParams[\"font.size\"] = 10\n\nACCENT = \"#2563eb\"\nINK = \"#111827\"\nMUTED = \"#6b7280\"\n\ndef tufte(ax):\n    for side in (\"top\", \"right\"):\n        ax.spines[side].set_visible(False)\n    for side in (\"left\", \"bottom\"):\n        ax.spines[side].set_color(\"#9ca3af\")\n    ax.tick_params(colors=MUTED, labelsize=9)\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_slider(\"r\", \"상관계수 r\", min=-1.0, max=1.0, value=0.7, step=0.05),\n        ui.input_slider(\"n\", \"점의 개수 n\", min=20, max=200, value=60, step=5),\n        ui.input_checkbox(\"trend\", \"추세선 보기\", False),\n        ui.input_action_button(\"regen\", \"다시 생성\"),\n        width=240,\n    ),\n    ui.card(\n        ui.card_header(\"산점도 — r이 점들의 모양을 정합니다\"),\n        ui.output_plot(\"cloud\", height=\"340px\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @reactive.calc\n    def points():\n        input.regen()  # 버튼을 누르면 새로 생성\n        r, n = input.r(), input.n()\n        x = np.random.normal(0, 1, n)\n        e = np.random.normal(0, 1, n)\n        y = r * x + sqrt(max(0.0, 1 - r * r)) * e\n        return x, y\n\n    @render.plot(alt=\"상관계수에 따른 산점도\")\n    def cloud():\n        x, y = points()\n\n        fig, ax = plt.subplots()\n        ax.scatter(x, y, s=28, color=ACCENT, alpha=0.7, zorder=2)\n        if input.trend():\n            slope, intercept = np.polyfit(x, y, 1)\n            xs = np.linspace(x.min(), x.max(), 2)\n            ax.plot(xs, slope * xs + intercept, color=INK, linewidth=1.5, zorder=3)\n        ax.set_xlabel(\"x\", color=MUTED)\n        ax.set_ylabel(\"y\", color=MUTED)\n        ax.set_xlim(-3.5, 3.5)\n        ax.set_ylim(-3.5, 3.5)\n        tufte(ax)\n        return fig\n\n    @render.ui\n    def readout():\n        x, y = points()\n        rs = float(np.corrcoef(x, y)[0, 1])\n\n        if abs(rs) &lt; 0.1:\n            desc = \"상관관계가 거의 없습니다\"\n        else:\n            direction = \"양\" if rs &gt; 0 else \"음\"\n            if abs(rs) &gt;= 0.7:\n                strength = \"강한\"\n            elif abs(rs) &gt;= 0.3:\n                strength = \"어느 정도의\"\n            else:\n                strength = \"약한\"\n            desc = f\"{strength} {direction}의 상관관계가 보입니다\"\n        return ui.HTML(\n            '&lt;p class=\"hs-readline\"&gt;'\n            f\"설정한 r = {input.r():.2f} · 이 표본에서 계산한 상관계수 = {rs:.3f} — {desc}. \"\n            \"점의 개수 n이 작을수록 두 값의 차이가 커지고, \"\n            \"'다시 생성'을 누를 때마다 표본상관계수가 조금씩 달라집니다.&lt;/p&gt;\"\n        )\n\napp = App(app_ui, server)\n\n\n상관계수가 \\(r\\)인 자료를 만들고 확인하는 핵심 코드입니다.\nimport numpy as np\nfrom math import sqrt\n\nr, n = 0.7, 60\nx = np.random.normal(0, 1, n)\ne = np.random.normal(0, 1, n)\ny = r * x + sqrt(1 - r * r) * e     # 상관계수가 약 r인 y\n\nprint(np.corrcoef(x, y)[0, 1])      # 표본상관계수 — r에 가까움\nprint(np.polyfit(x, y, 1))          # 추세선의 기울기와 절편"
  },
  {
    "objectID": "pages/01_data/scatter/index.html#스스로-확인",
    "href": "pages/01_data/scatter/index.html#스스로-확인",
    "title": "산점도와 상관관계",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — r = 1(또는 −1)로 맞추면 점들은 어떤 모양이 되는가?\n\n\n\n\n\n모든 점이 하나의 직선 위에 완벽하게 늘어섭니다. \\(r = 1\\)이면 오른쪽 위로, \\(r = -1\\)이면 오른쪽 아래로 향하는 직선입니다. 상관계수의 절댓값은 “점들이 직선에 얼마나 가깝게 모이는가”를 재는 수임을 확인해 보세요.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — r = 0.3, n = 20으로 두고 ’다시 생성’을 여러 번 누르면 계산된 상관계수는 얼마나 출렁이는가?\n\n\n\n\n\n표본이 작으면 계산된 상관계수가 0 근처부터 0.6 이상까지 크게 출렁입니다. 심지어 부호가 뒤집히기도 합니다. 적은 자료에서 관찰한 상관관계를 성급하게 믿으면 안 되는 이유입니다. n을 200으로 키워 비교해 보세요.\n\n\n\n\n← 이전: 산포도와 상자그림 다음: 순열과 조합 →"
  },
  {
    "objectID": "pages/01_data/structure/index.html",
    "href": "pages/01_data/structure/index.html",
    "title": "정보의 구조화",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/01_data/structure/index.html#개념",
    "href": "pages/01_data/structure/index.html#개념",
    "title": "정보의 구조화",
    "section": "개념",
    "text": "개념\n같은 내용이라도 어떻게 구조화하느냐에 따라 전달력이 크게 달라집니다. 잘 정리된 정보는 의미가 한눈에 들어오지만, 뒤섞인 정보는 뜻이 잘못 전달되기도 합니다. 중등 「정보」 교과서는 정보를 정리하는 네 가지 대표 형태를 소개합니다.\n\n순차적 구조 — 내용을 순서대로 나열합니다. 쉽고 빠르지만 양이 많아지면 관리가 어렵습니다.\n표(테이블) 구조 — 행과 열로 나눕니다. 시간표·출석부처럼 항목별 비교에 강합니다.\n계층형 구조 — 수준별로 묶습니다. 조직도·목차·폴더처럼 큰 것에서 작은 것으로 내려갑니다.\n다이어그램 구조 — 기호·선·점으로 관계와 크기를 보여 줍니다. 그래프가 여기에 속합니다.\n\n\n\n“우리나라 시도별 인구수를 어떻게 하면 효과적으로 전달할까?” — 교과서가 던지는 이 질문이 바로 통계 그래프의 출발점입니다. 정답은 하나가 아니라 활용 목적에 따라 달라집니다.\n핵심은 어느 하나가 항상 옳은 것이 아니라, 정보의 활용 목적에 맞는 형태를 고르는 것입니다. 개별 값을 정확히 찾아야 하면 표가, 소속 관계를 보여야 하면 계층이, 크기를 한눈에 비교하려면 다이어그램이 유리합니다. 아래 앱에서 같은 인구 데이터를 네 형태로 바꿔 보며 차이를 느껴 보세요."
  },
  {
    "objectID": "pages/01_data/structure/index.html#만지며-배우기",
    "href": "pages/01_data/structure/index.html#만지며-배우기",
    "title": "정보의 구조화",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 640\n\nfrom shiny import App, render, ui\n\nACCENT = \"#2563eb\"\n\n# 시도를 권역으로 묶은 계층 구조 (인구는 백만 명 단위, 교과서 표 기준 반올림)\nREGIONS = {\n    \"수도권\": [(\"서울\", 10.0), (\"인천\", 2.9), (\"경기\", 12.5)],\n    \"충청권\": [(\"대전\", 1.5), (\"세종\", 0.2), (\"충북\", 1.6), (\"충남\", 2.1)],\n    \"호남권\": [(\"광주\", 1.5), (\"전북\", 1.9), (\"전남\", 1.9)],\n    \"영남권\": [(\"부산\", 3.5), (\"대구\", 2.5), (\"울산\", 1.2),\n             (\"경북\", 2.7), (\"경남\", 3.4)],\n    \"강원권\": [(\"강원\", 1.5)],\n    \"제주권\": [(\"제주\", 0.6)],\n}\nFLAT = [(name, pop) for cities in REGIONS.values() for (name, pop) in cities]\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_radio_buttons(\n            \"form\", \"구조화 형태\",\n            {\"seq\": \"순차적\", \"table\": \"표(테이블)\",\n             \"tree\": \"계층형\", \"diagram\": \"다이어그램\"},\n            selected=\"table\",\n        ),\n        width=250,\n    ),\n    ui.card(\n        ui.card_header(\"같은 데이터, 네 가지 구조 — 목적에 맞는 형태는?\"),\n        ui.output_ui(\"view\"),\n    ),\n    ui.card(ui.output_ui(\"note\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @render.ui\n    def view():\n        form = input.form()\n\n        if form == \"seq\":\n            items = \" → \".join(f\"{n} {p}\" for n, p in FLAT)\n            return ui.HTML(\n                '&lt;p style=\"line-height:2; font-size:0.95em;\"&gt;' + items + \"&lt;/p&gt;\"\n            )\n\n        if form == \"table\":\n            td = ('style=\"border:1px solid #d1d5db; padding:3px 12px;'\n                  ' text-align:center;\"')\n            rows = \"\".join(\n                f\"&lt;tr&gt;&lt;td {td}&gt;{n}&lt;/td&gt;&lt;td {td}&gt;{p:.1f}&lt;/td&gt;&lt;/tr&gt;\"\n                for n, p in FLAT\n            )\n            return ui.HTML(\n                '&lt;table style=\"border-collapse:collapse; margin:0 auto;'\n                ' font-size:0.9em;\"&gt;'\n                f\"&lt;tr&gt;&lt;th {td}&gt;시도&lt;/th&gt;&lt;th {td}&gt;인구(백만)&lt;/th&gt;&lt;/tr&gt;\"\n                f\"{rows}&lt;/table&gt;\"\n            )\n\n        if form == \"tree\":\n            blocks = \"\"\n            for region, cities in REGIONS.items():\n                total = sum(p for _, p in cities)\n                leaves = \"\".join(\n                    f'&lt;li style=\"margin-left:1.4em;\"&gt;{n} — {p:.1f}백만&lt;/li&gt;'\n                    for n, p in cities\n                )\n                blocks += (\n                    f'&lt;li style=\"font-weight:600; margin-top:0.4em;\"&gt;'\n                    f\"{region} ({total:.1f}백만)&lt;/li&gt;{leaves}\"\n                )\n            return ui.HTML(\n                f'&lt;ul style=\"list-style:none; padding-left:0.5em;'\n                f' line-height:1.7; font-size:0.92em;\"&gt;{blocks}&lt;/ul&gt;'\n            )\n\n        # diagram — 권역별 합계를 HTML 막대로\n        totals = {r: sum(p for _, p in c) for r, c in REGIONS.items()}\n        mx = max(totals.values())\n        bars = \"\"\n        for region, t in sorted(totals.items(), key=lambda x: -x[1]):\n            w = t / mx * 100\n            bars += (\n                '&lt;div style=\"display:flex; align-items:center; margin:6px 0;\"&gt;'\n                f'&lt;div style=\"width:70px; text-align:right; padding-right:10px;'\n                f' font-size:0.88em;\"&gt;{region}&lt;/div&gt;'\n                f'&lt;div style=\"height:20px; width:{w:.1f}%; background:{ACCENT};'\n                f' border-radius:3px;\"&gt;&lt;/div&gt;'\n                f'&lt;div style=\"padding-left:8px; font-size:0.85em;'\n                f' color:#6b7280;\"&gt;{t:.1f}&lt;/div&gt;&lt;/div&gt;'\n            )\n        return ui.HTML(f'&lt;div style=\"padding:0.5em 0;\"&gt;{bars}&lt;/div&gt;')\n\n    @render.ui\n    def note():\n        msg = {\n            \"seq\": \"순차적 구조는 빠르게 나열하지만, 17개가 한 줄로 늘어서면 \"\n                   \"어디에 무엇이 있는지 찾기 어렵습니다.\",\n            \"table\": \"표는 개별 시도의 정확한 값을 찾기에 가장 좋습니다. \"\n                     \"대신 전체 크기를 한눈에 비교하기는 어렵습니다.\",\n            \"tree\": \"계층형은 '어느 권역에 속하는가'라는 소속 관계를 드러냅니다. \"\n                    \"권역별 합계까지 함께 보여 줄 수 있습니다.\",\n            \"diagram\": \"다이어그램(막대)은 크기 비교가 즉각적입니다. \"\n                       \"수도권이 다른 권역을 압도한다는 사실이 바로 읽힙니다. \"\n                       \"통계 그래프가 여기에 해당합니다.\",\n        }[input.form()]\n        return ui.HTML(f'&lt;p class=\"hs-readline\"&gt;{msg}&lt;/p&gt;')\n\napp = App(app_ui, server)\n\n\n계층형 구조는 딕셔너리로, 권역별 합계는 반복문으로 간단히 표현됩니다.\nregions = {\n    \"수도권\": {\"서울\": 10.0, \"인천\": 2.9, \"경기\": 12.5},\n    \"영남권\": {\"부산\": 3.5, \"대구\": 2.5, \"울산\": 1.2, \"경북\": 2.7, \"경남\": 3.4},\n}\n\nfor region, cities in regions.items():\n    total = sum(cities.values())         # 권역 = 상위 계층\n    print(f\"{region}: {total:.1f}백만\")   # 시도 = 하위 계층"
  },
  {
    "objectID": "pages/01_data/structure/index.html#스스로-확인",
    "href": "pages/01_data/structure/index.html#스스로-확인",
    "title": "정보의 구조화",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — ’특정 시의 정확한 인구’를 알려면 어떤 구조가 좋은가?\n\n\n\n\n\n개별 값을 정확히 찾는 데는 표(테이블) 구조가 가장 적합합니다. 다이어그램은 크기를 눈대중으로 비교하기엔 좋지만 정확한 숫자를 읽기는 어렵고, 순차 구조는 항목이 많으면 원하는 값을 찾기 번거롭습니다.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — 뉴스에서 지역별 인구를 한 장의 그림으로 전할 때 왜 다이어그램(그래프)을 쓰는가?\n\n\n\n\n\n독자에게 필요한 것은 정확한 자릿수가 아니라 “어디가 많고 어디가 적은가” 라는 크기 관계이기 때문입니다. 다이어그램은 이 비교를 순식간에 전달합니다. 활용 목적이 ’정확한 조회’가 아니라 ’한눈에 비교’일 때 다이어그램이 가장 효과적입니다.\n\n\n\n\n← 이전: 자료와 정보 다음: 도수분포표와 히스토그램 →"
  },
  {
    "objectID": "pages/02_prob/binom_thm/index.html",
    "href": "pages/02_prob/binom_thm/index.html",
    "title": "이항정리",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/02_prob/binom_thm/index.html#개념",
    "href": "pages/02_prob/binom_thm/index.html#개념",
    "title": "이항정리",
    "section": "개념",
    "text": "개념\n\\((a+b)^n\\)을 펼치면 각 항은 \\(n\\)개의 괄호 \\((a+b)\\)에서 \\(a\\) 또는 \\(b\\)를 하나씩 골라 곱한 것입니다. \\(b\\)를 \\(r\\)번 고르는 방법이 곧 \\({}_n\\mathrm{C}_r\\)가지이므로, \\(a^{\\,n-r}b^{\\,r}\\)의 계수는 \\({}_n\\mathrm{C}_r\\)가 됩니다. 이것이 이항정리입니다.\n\\[(a+b)^n = \\sum_{r=0}^{n} {}_n\\mathrm{C}_r\\, a^{\\,n-r} b^{\\,r}\\]\n\n\n계수 \\({}_n\\mathrm{C}_0,\\,{}_n\\mathrm{C}_1,\\,\\dots,\\,{}_n\\mathrm{C}_n\\)을 이항계수라고 합니다. 조합의 수가 전개식의 계수로 그대로 나타나는 것이 경우의 수와 대수를 잇는 다리입니다.\n이항계수를 위에서부터 차례로 쌓으면 파스칼의 삼각형이 됩니다. 각 수는 바로 위 두 수의 합이고(\\({}_{n-1}\\mathrm{C}_{r-1} + {}_{n-1}\\mathrm{C}_{r} = {}_n\\mathrm{C}_r\\)), 좌우 대칭입니다(\\({}_n\\mathrm{C}_r = {}_n\\mathrm{C}_{n-r}\\)). 아래 앱에서 차수 \\(n\\)을 바꾸며 삼각형의 한 줄이 \\((a+b)^n\\)의 계수와 정확히 일치함을 확인해 보세요."
  },
  {
    "objectID": "pages/02_prob/binom_thm/index.html#만지며-배우기",
    "href": "pages/02_prob/binom_thm/index.html#만지며-배우기",
    "title": "이항정리",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 640\n\nfrom math import comb\nfrom shiny import App, render, ui\n\nACCENT = \"#2563eb\"\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_slider(\"n\", \"차수 n — (a+b)ⁿ\", min=1, max=8, value=4, step=1),\n        width=250,\n    ),\n    ui.card(\n        ui.card_header(\"파스칼의 삼각형 — 파란 줄이 (a+b)ⁿ의 계수입니다\"),\n        ui.output_ui(\"triangle\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @render.ui\n    def triangle():\n        n = input.n()\n        rows = \"\"\n        for i in range(n + 1):\n            hi = (i == n)\n            cells = \"\"\n            for r in range(i + 1):\n                c = comb(i, r)\n                color = \"white\" if hi else \"#374151\"\n                bg = ACCENT if hi else \"transparent\"\n                cells += (\n                    f'&lt;span style=\"display:inline-block; min-width:34px;'\n                    f' margin:2px; padding:2px 4px; text-align:center;'\n                    f' border-radius:4px; background:{bg}; color:{color};'\n                    f' font-size:0.85em;\"&gt;{c}&lt;/span&gt;'\n                )\n            rows += f'&lt;div style=\"white-space:nowrap;\"&gt;{cells}&lt;/div&gt;'\n        return ui.HTML(\n            '&lt;div style=\"text-align:center; overflow-x:auto;'\n            f' padding:0.5em 0;\"&gt;{rows}&lt;/div&gt;'\n        )\n\n    @render.ui\n    def readout():\n        n = input.n()\n        coeffs = [comb(n, r) for r in range(n + 1)]\n        terms = []\n        for r in range(n + 1):\n            c = coeffs[r]\n            cs = \"\" if c == 1 else str(c)\n            ap = \"\" if n - r == 0 else (\"a\" if n - r == 1 else f\"a^{n-r}\")\n            bp = \"\" if r == 0 else (\"b\" if r == 1 else f\"b^{r}\")\n            body = (ap + bp) or \"1\"\n            terms.append(cs + body)\n        expr = \" + \".join(terms)\n        return ui.HTML(\n            '&lt;p class=\"hs-readline\"&gt;'\n            f\"(a+b)^{n} 의 계수는 {coeffs} 입니다. 삼각형의 아래에서 첫째 줄(파란색)과 \"\n            f\"정확히 같습니다.&lt;br&gt;&lt;b&gt;(a+b)^{n} = {expr}&lt;/b&gt;&lt;br&gt;\"\n            f\"계수를 모두 더하면 {sum(coeffs):,} = 2^{n} 인데, 이는 (a+b)에 a=b=1을 \"\n            f\"넣은 값과 같습니다.&lt;/p&gt;\"\n        )\n\napp = App(app_ui, server)\n\n\n이항계수는 조합 함수 comb 로 한 줄에 얻습니다.\nfrom math import comb\n\nn = 5\ncoeffs = [comb(n, r) for r in range(n + 1)]\nprint(coeffs)                        # [1, 5, 10, 10, 5, 1]\n\n# (x+2)^4 에서 x^2 의 계수: r=2 항의 계수 × 2^2\nprint(comb(4, 2) * 2 ** 2)           # 6 × 4 = 24"
  },
  {
    "objectID": "pages/02_prob/binom_thm/index.html#스스로-확인",
    "href": "pages/02_prob/binom_thm/index.html#스스로-확인",
    "title": "이항정리",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — (x+2)⁴ 의 전개식에서 x² 의 계수는?\n\n\n\n\n\n\\(a=x,\\,b=2,\\,n=4\\)에서 \\(x^2\\) 항은 \\(r=2\\)일 때이므로 계수는 \\({}_4\\mathrm{C}_2 \\times 2^2 = 6 \\times 4 = 24\\)입니다. 파스칼의 삼각형 넷째 줄 \\(1,4,6,4,1\\)에서 가운데 \\(6\\)에 \\(b=2\\)의 거듭제곱 \\(2^2\\)를 곱한 값입니다.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — 이항계수를 한 줄 모두 더하면 왜 2ⁿ 이 되는가?\n\n\n\n\n\n이항정리 \\((a+b)^n = \\sum {}_n\\mathrm{C}_r\\,a^{n-r}b^r\\)에 \\(a=b=1\\)을 넣으면 좌변은 \\(2^n\\), 우변은 이항계수의 합이 됩니다. 즉 \\({}_n\\mathrm{C}_0 + {}_n\\mathrm{C}_1 + \\cdots + {}_n\\mathrm{C}_n = 2^n\\)입니다. 이는 원소가 \\(n\\)개인 집합의 부분집합이 \\(2^n\\)개인 것과도 같은 사실입니다.\n\n\n\n\n← 이전: 중복조합 다음: 확률의 뜻 →"
  },
  {
    "objectID": "pages/02_prob/conditional/index.html",
    "href": "pages/02_prob/conditional/index.html",
    "title": "조건부확률과 독립",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/02_prob/conditional/index.html#개념",
    "href": "pages/02_prob/conditional/index.html#개념",
    "title": "조건부확률과 독립",
    "section": "개념",
    "text": "개념\n사건 \\(B\\)가 일어났다는 것을 알 때 사건 \\(A\\)가 일어날 확률을 조건부확률이라 하고, 다음과 같이 정의합니다.\n\\[P(A \\mid B) = \\frac{P(A \\cap B)}{P(B)} \\quad (P(B) &gt; 0)\\]\n\n\n“비 오는 날”이라는 정보를 알면 “지각”의 확률이 달라지듯, 조건부확률은 새 정보로 확률을 다시 재는 것입니다.\n조건 \\(B\\)를 알아도 \\(A\\)의 확률이 변하지 않으면, 즉 \\(P(A \\mid B) = P(A)\\)이면 두 사건은 서로 독립이라 하고, 이때 \\(P(A \\cap B) = P(A)\\,P(B)\\)가 성립합니다. 그렇지 않으면 서로 종속입니다. 아래 앱은 학생 100명을 정사각형 100칸으로 나타냅니다. 사건 \\(A\\)는 “아침 운동을 함”, 사건 \\(B\\)는 “아침밥을 먹음”입니다. \\(P(A)\\), \\(P(B \\mid A)\\), \\(P(B \\mid A^c)\\)를 움직여 네 영역의 칸 수가 어떻게 변하는지, 그리고 언제 두 사건이 독립이 되는지 살펴보세요."
  },
  {
    "objectID": "pages/02_prob/conditional/index.html#만지며-배우기",
    "href": "pages/02_prob/conditional/index.html#만지며-배우기",
    "title": "조건부확률과 독립",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 660\n\nfrom shiny import App, render, ui\nimport numpy as np\nimport matplotlib\nimport matplotlib.pyplot as plt\nfrom matplotlib.patches import Rectangle\n\nmatplotlib.rcParams[\"font.size\"] = 10\n\nACCENT = \"#2563eb\"\nINK = \"#111827\"\nMUTED = \"#6b7280\"\n\ndef tufte(ax):\n    for side in (\"top\", \"right\"):\n        ax.spines[side].set_visible(False)\n    for side in (\"left\", \"bottom\"):\n        ax.spines[side].set_color(\"#9ca3af\")\n    ax.tick_params(colors=MUTED, labelsize=9)\n\ndef counts(pa, pba, pbac):\n    n_a = round(100 * pa)\n    n_ab = min(round(100 * pa * pba), n_a)\n    n_acb = min(round(100 * (1 - pa) * pbac), 100 - n_a)\n    return n_a, n_ab, n_acb\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_slider(\"pa\", \"P(A) — 아침 운동을 하는 비율\",\n                        min=0.1, max=0.9, value=0.4, step=0.05),\n        ui.input_slider(\"pba\", \"P(B|A) — 운동하는 학생 중 아침밥\",\n                        min=0.0, max=1.0, value=0.8, step=0.05),\n        ui.input_slider(\"pbac\", \"P(B|Aᶜ) — 운동 안 하는 학생 중 아침밥\",\n                        min=0.0, max=1.0, value=0.5, step=0.05),\n        width=240,\n    ),\n    ui.card(\n        ui.card_header(\"학생 100명 — A: 아침 운동, B: 아침밥\"),\n        ui.output_plot(\"grid\", height=\"340px\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @render.plot(alt=\"100명의 학생을 네 영역으로 나눈 격자\")\n    def grid():\n        n_a, n_ab, n_acb = counts(input.pa(), input.pba(), input.pbac())\n\n        fig, ax = plt.subplots()\n        for i in range(100):\n            x, y = i % 10, i // 10\n            if i &lt; n_ab:                       # A∩B\n                fc, ec, lw = ACCENT, ACCENT, 0.8\n            elif i &lt; n_a:                      # A∩Bᶜ\n                fc, ec, lw = \"none\", ACCENT, 1.2\n            elif i &lt; n_a + n_acb:              # Aᶜ∩B\n                fc, ec, lw = \"#9ca3af\", \"#9ca3af\", 0.8\n            else:                              # Aᶜ∩Bᶜ\n                fc, ec, lw = \"none\", \"#d1d5db\", 0.8\n            ax.add_patch(Rectangle((x, 9 - y), 0.88, 0.88,\n                                   facecolor=fc, edgecolor=ec, linewidth=lw))\n\n        legend = [\n            (f\"A∩B ({n_ab})\", ACCENT, ACCENT, 0.8),\n            (f\"A only ({n_a - n_ab})\", \"none\", ACCENT, 1.2),\n            (f\"B only ({n_acb})\", \"#9ca3af\", \"#9ca3af\", 0.8),\n            (f\"neither ({100 - n_a - n_acb})\", \"none\", \"#d1d5db\", 0.8),\n        ]\n        for j, (label, fc, ec, lw) in enumerate(legend):\n            yy = 8.2 - 1.6 * j\n            ax.add_patch(Rectangle((10.8, yy), 0.7, 0.7,\n                                   facecolor=fc, edgecolor=ec, linewidth=lw))\n            ax.text(11.8, yy + 0.35, label, va=\"center\",\n                    fontsize=9, color=INK)\n\n        ax.set_xlim(-0.3, 16.5)\n        ax.set_ylim(-0.3, 10.2)\n        ax.set_aspect(\"equal\")\n        ax.set_xticks([])\n        ax.set_yticks([])\n        for side in (\"top\", \"right\", \"left\", \"bottom\"):\n            ax.spines[side].set_visible(False)\n        return fig\n\n    @render.ui\n    def readout():\n        pa, pba, pbac = input.pa(), input.pba(), input.pbac()\n        pb = pa * pba + (1 - pa) * pbac\n        if pb == 0:\n            return ui.HTML(\n                '&lt;p class=\"hs-readline\"&gt;P(B) = 0이라 조건부확률 '\n                \"P(A|B)는 정의되지 않습니다. P(B|A)나 P(B|Aᶜ)를 \"\n                \"0보다 크게 올려 보세요.&lt;/p&gt;\"\n            )\n        pab = pa * pba / pb\n        if abs(pba - pb) &lt; 0.005:\n            indep = (\"P(B|A)와 P(B)가 같으므로 두 사건은 서로 \"\n                     \"독립입니다 — 운동 여부가 아침밥 확률을 바꾸지 않습니다.\")\n        elif pba &gt; pb:\n            indep = (f\"P(B|A) = {pba:.2f} &gt; P(B) = {pb:.2f}이므로 두 사건은 \"\n                     \"서로 종속입니다 — 운동하는 학생이 아침밥을 더 잘 먹습니다.\")\n        else:\n            indep = (f\"P(B|A) = {pba:.2f} &lt; P(B) = {pb:.2f}이므로 두 사건은 \"\n                     \"서로 종속입니다 — 운동하는 학생이 아침밥을 덜 먹습니다.\")\n        return ui.HTML(\n            f'&lt;p class=\"hs-readline\"&gt;'\n            f\"전체에서 아침밥을 먹을 확률은 P(B) = P(A)P(B|A) + P(Aᶜ)P(B|Aᶜ) = \"\n            f\"{pb:.2f}입니다. 아침밥을 먹은 학생이 운동도 했을 조건부확률은 \"\n            f\"P(A|B) = P(A)P(B|A)/P(B) = {pab:.2f}입니다. {indep}&lt;/p&gt;\"\n        )\n\napp = App(app_ui, server)\n\n\n앱의 핵심 계산은 이 몇 줄이 전부입니다.\npa = 0.4            # P(A): 아침 운동\npba = 0.8           # P(B|A)\npbac = 0.5          # P(B|Aᶜ)\n\npb = pa * pba + (1 - pa) * pbac   # 전체확률 P(B) = 0.62\npab = pa * pba / pb               # P(A|B) ≈ 0.516 (베이즈)\n\nprint(pb, pab)\nprint(\"independent\" if abs(pba - pb) &lt; 1e-9 else \"dependent\")"
  },
  {
    "objectID": "pages/02_prob/conditional/index.html#스스로-확인",
    "href": "pages/02_prob/conditional/index.html#스스로-확인",
    "title": "조건부확률과 독립",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — P(B|A)와 P(B|Aᶜ)를 같은 값으로 맞추면 무슨 일이 일어나는가?\n\n\n\n\n\n두 값이 같으면 \\(P(B) = P(B \\mid A) = P(B \\mid A^c)\\)가 되어 \\(A\\)와 \\(B\\)는 서로 독립이 됩니다. 운동을 하든 안 하든 아침밥을 먹을 확률이 같다는 뜻입니다. 격자에서 \\(A\\) 영역과 \\(A^c\\) 영역의 색칠 비율이 같아지는 것을 확인해 보세요.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — 기본값(P(A)=0.4, P(B|A)=0.8, P(B|Aᶜ)=0.5)에서 P(A|B)를 손으로 구해 보라.\n\n\n\n\n\n전체확률로 \\(P(B) = 0.4 \\times 0.8 + 0.6 \\times 0.5 = 0.62\\), 따라서 \\(P(A \\mid B) = \\dfrac{P(A \\cap B)}{P(B)} = \\dfrac{0.32}{0.62} \\approx 0.52\\) 입니다. 아침밥을 먹었다는 정보만으로 운동했을 확률이 \\(0.4\\)에서 \\(0.52\\)로 올라갑니다 — 정보가 확률을 갱신한 것입니다.\n\n\n\n\n← 이전: 확률의 덧셈정리 다음: 확률변수와 기댓값 →"
  },
  {
    "objectID": "pages/02_prob/perm_various/index.html",
    "href": "pages/02_prob/perm_various/index.html",
    "title": "여러 가지 순열",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/02_prob/perm_various/index.html#개념",
    "href": "pages/02_prob/perm_various/index.html#개념",
    "title": "여러 가지 순열",
    "section": "개념",
    "text": "개념\n기본 순열을 세 방향으로 넓히면 자리 배치 문제 대부분을 다룰 수 있습니다.\n원순열은 서로 다른 것을 원형으로 늘어놓는 배열입니다. 원형에서는 전체를 돌려 겹치는 배치를 같은 것으로 보므로, 한 자리를 고정하고 나머지를 세면 됩니다.\n\\[\\text{서로 다른 } n\\text{개의 원순열} = \\frac{n!}{n} = (n-1)!\\]\n\n\n회전 대칭이 핵심입니다. 일렬 배치 \\(n!\\)가지는 원형에서 \\(n\\)개씩 같은 배치로 겹치므로, 그만큼 나눠 준 것이 \\((n-1)!\\)입니다.\n중복순열은 같은 것을 다시 뽑아도 되는 나열입니다. 서로 다른 \\(n\\)개에서 중복을 허용해 \\(r\\)개를 뽑아 늘어놓으면, 매 자리마다 \\(n\\)가지 선택이 있으므로\n\\[{}_n\\Pi_r = n^r\\]\n같은 것이 있는 순열은 구별되지 않는 것이 섞인 나열입니다. \\(n\\)개 중 같은 것이 각각 \\(p,\\,q,\\,\\dots,\\,r\\)개 있으면, 자기들끼리 자리를 바꿔도 같은 배열이므로 그만큼 나눕니다.\n\\[\\frac{n!}{p!\\,q!\\cdots r!}\\quad(p+q+\\cdots+r=n)\\]\n아래 앱은 이 중 원순열을 눈으로 보여 줍니다. 사람 수를 바꾸며 원형 배치 수 \\((n-1)!\\)가 일렬 배치 수 \\(n!\\)의 \\(\\tfrac{1}{n}\\)임을 확인해 보세요."
  },
  {
    "objectID": "pages/02_prob/perm_various/index.html#만지며-배우기",
    "href": "pages/02_prob/perm_various/index.html#만지며-배우기",
    "title": "여러 가지 순열",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 640\n\nfrom math import factorial\nfrom shiny import App, render, ui\nimport numpy as np\nimport matplotlib\nimport matplotlib.pyplot as plt\n\nmatplotlib.rcParams[\"font.size\"] = 10\nACCENT = \"#2563eb\"\nINK = \"#111827\"\nMUTED = \"#6b7280\"\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_slider(\"n\", \"원형 탁자에 앉는 사람 수\", min=3, max=9, value=5, step=1),\n        width=250,\n    ),\n    ui.card(\n        ui.card_header(\"원형 배치 — 통째로 돌리면 같은 배치입니다\"),\n        ui.output_plot(\"circle\", height=\"330px\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @render.plot(alt=\"원형으로 앉은 사람들의 자리 번호\")\n    def circle():\n        n = input.n()\n        ang = np.linspace(0, 2 * np.pi, n, endpoint=False) + np.pi / 2\n        x, y = np.cos(ang), np.sin(ang)\n\n        fig, ax = plt.subplots()\n        t = np.linspace(0, 2 * np.pi, 200)\n        ax.plot(np.cos(t), np.sin(t), color=\"#d1d5db\", linewidth=1)\n        ax.scatter(x, y, s=520, color=ACCENT, alpha=0.9, zorder=3)\n        for i, (xi, yi) in enumerate(zip(x, y)):\n            ax.text(xi, yi, str(i + 1), color=\"white\", ha=\"center\",\n                    va=\"center\", fontsize=11, fontweight=\"bold\", zorder=4)\n        ax.set_xlim(-1.3, 1.3)\n        ax.set_ylim(-1.3, 1.3)\n        ax.set_aspect(\"equal\")\n        ax.axis(\"off\")\n        return fig\n\n    @render.ui\n    def readout():\n        n = input.n()\n        circular = factorial(n - 1)\n        linear = factorial(n)\n        return ui.HTML(\n            '&lt;p class=\"hs-readline\"&gt;'\n            f\"{n}명을 원형으로 앉히는 방법은 (n-1)! = {n - 1}! = &lt;b&gt;{circular:,}가지&lt;/b&gt;입니다. \"\n            f\"같은 사람들을 일렬로 세우면 n! = {n}! = {linear:,}가지지만, \"\n            f\"원형에서는 통째로 돌린 {n}가지가 모두 한 배치로 겹칩니다. \"\n            f\"그래서 {linear:,} ÷ {n} = {circular:,}가지가 됩니다.&lt;/p&gt;\"\n        )\n\napp = App(app_ui, server)\n\n\n세 가지 순열 모두 factorial 로 바로 계산됩니다.\nfrom math import factorial\n\n# 원순열: 6명을 원탁에 앉히기\nprint(factorial(6 - 1))              # (n-1)! = 120\n\n# 중복순열: 0~9 숫자로 4자리 잠금번호\nprint(10 ** 4)                       # n^r = 10000\n\n# 같은 것이 있는 순열: 앞3·뒤2로 동전 5개 늘어놓기\nprint(factorial(5) // (factorial(3) * factorial(2)))   # 5!/(3!2!) = 10"
  },
  {
    "objectID": "pages/02_prob/perm_various/index.html#스스로-확인",
    "href": "pages/02_prob/perm_various/index.html#스스로-확인",
    "title": "여러 가지 순열",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — 6명을 원탁에 앉히는 방법과 일렬로 세우는 방법은 몇 배 차이인가?\n\n\n\n\n\n원순열은 \\((6-1)! = 120\\)가지, 일렬 순열은 \\(6! = 720\\)가지로 정확히 6배 차이입니다. 원형에서는 전체를 한 칸씩 돌린 6가지 배치가 모두 같은 것으로 묶이기 때문에, 일렬 배치 수를 자리 수 6으로 나눈 값이 됩니다.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — 빨간 공 3개와 파란 공 2개를 일렬로 놓는 방법은 몇 가지인가?\n\n\n\n\n\n색이 같은 공끼리는 구별되지 않으므로 같은 것이 있는 순열입니다. \\(\\dfrac{5!}{3!\\,2!} = \\dfrac{120}{12} = 10\\)가지입니다. 만약 다섯 공이 모두 색이 달랐다면 \\(5! = 120\\)가지지만, 같은 색끼리 자리를 바꿔도 구별되지 않는 만큼(\\(3!\\times2!\\)배) 나눠 준 것입니다.\n\n\n\n\n← 이전: 순열과 조합 다음: 중복조합 →"
  },
  {
    "objectID": "pages/03_dist/binomial/index.html",
    "href": "pages/03_dist/binomial/index.html",
    "title": "이항분포",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/03_dist/binomial/index.html#개념",
    "href": "pages/03_dist/binomial/index.html#개념",
    "title": "이항분포",
    "section": "개념",
    "text": "개념\n한 번의 시행에서 어떤 사건이 일어날 확률이 \\(p\\)로 일정할 때, 독립적으로 \\(n\\)번 반복한 시행에서 그 사건이 일어나는 횟수 \\(X\\)의 분포를 이항분포 \\(B(n,\\,p)\\)라고 합니다.\n\\[P(X = k) = \\binom{n}{k} p^k (1-p)^{n-k}, \\qquad\nE(X) = np, \\qquad \\sigma = \\sqrt{np(1-p)}\\]\n\n\n자유투 성공률이 \\(p\\)인 선수가 \\(n\\)번 던질 때의 성공 횟수, \\(n\\)문항 오지선다를 전부 찍었을 때의 정답 개수가 모두 이항분포입니다.\n\\(n\\)이 충분히 크면 이항분포는 정규분포 \\(N(np,\\; np(1-p))\\)에 가까워집니다. 아래 앱에서 \\(n\\)을 키우며 막대들이 종 모양 곡선에 붙는 모습을 확인해 보세요."
  },
  {
    "objectID": "pages/03_dist/binomial/index.html#만지며-배우기",
    "href": "pages/03_dist/binomial/index.html#만지며-배우기",
    "title": "이항분포",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 640\n\nfrom math import comb, sqrt, floor, ceil\nfrom shiny import App, render, ui\nimport numpy as np\nimport matplotlib\nimport matplotlib.pyplot as plt\n\nmatplotlib.rcParams[\"font.size\"] = 10\n\nACCENT = \"#2563eb\"\nINK = \"#111827\"\nMUTED = \"#6b7280\"\n\ndef tufte(ax):\n    for side in (\"top\", \"right\"):\n        ax.spines[side].set_visible(False)\n    for side in (\"left\", \"bottom\"):\n        ax.spines[side].set_color(\"#9ca3af\")\n    ax.tick_params(colors=MUTED, labelsize=9)\n\ndef pmf(n, p):\n    return np.array([comb(n, k) * p**k * (1 - p)**(n - k) for k in range(n + 1)])\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_slider(\"n\", \"시행 횟수 n\", min=1, max=80, value=20, step=1),\n        ui.input_slider(\"p\", \"확률 p\", min=0.05, max=0.95, value=0.5, step=0.05),\n        ui.input_checkbox(\"approx\", \"정규분포 근사 곡선 겹쳐 보기\", False),\n        width=240,\n    ),\n    ui.card(\n        ui.card_header(\"이항분포 B(n, p)\"),\n        ui.output_plot(\"pmf_plot\", height=\"340px\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @render.plot(alt=\"이항분포 확률질량함수\")\n    def pmf_plot():\n        n, p = input.n(), input.p()\n        probs = pmf(n, p)\n        mean, sd = n * p, sqrt(n * p * (1 - p))\n\n        # 보기 좋은 구간만 표시 (평균 ± 4σ)\n        lo = max(0, floor(mean - 4 * sd))\n        hi = min(n, ceil(mean + 4 * sd))\n        ks = np.arange(lo, hi + 1)\n\n        fig, ax = plt.subplots()\n        ax.bar(ks, probs[lo:hi + 1], width=0.8, color=ACCENT, alpha=0.85)\n        ax.axvline(mean, color=INK, linewidth=0.8, linestyle=\":\")\n        ax.text(mean, ax.get_ylim()[1] * 0.97, \" E(X)=np\",\n                color=INK, fontsize=9, va=\"top\")\n\n        if input.approx():\n            xs = np.linspace(lo - 0.5, hi + 0.5, 300)\n            ys = np.exp(-((xs - mean) ** 2) / (2 * sd * sd)) / (sd * sqrt(2 * np.pi))\n            ax.plot(xs, ys, color=INK, linewidth=1.4)\n\n        ax.set_xlabel(\"k\", color=MUTED)\n        ax.set_ylabel(\"P(X = k)\", color=MUTED)\n        tufte(ax)\n        return fig\n\n    @render.ui\n    def readout():\n        n, p = input.n(), input.p()\n        mean, sd = n * p, sqrt(n * p * (1 - p))\n        probs = pmf(n, p)\n        within = probs[max(0, ceil(mean - sd)):floor(mean + sd) + 1].sum()\n        return ui.HTML(\n            f'&lt;p class=\"hs-readline\"&gt;'\n            f\"E(X) = np = {mean:.1f} · σ = √np(1−p) = {sd:.2f} — \"\n            f\"X가 평균 ± σ 안에 있을 확률은 약 {within:.2f}입니다. \"\n            f\"n을 키우면 막대가 종 모양에 가까워집니다.&lt;/p&gt;\"\n        )\n\napp = App(app_ui, server)\n\n\nfrom math import comb, sqrt\n\nn, p = 20, 0.5\n\n# P(X = k), k = 0..n\nprobs = [comb(n, k) * p**k * (1 - p)**(n - k) for k in range(n + 1)]\n\nE = n * p                    # 기댓값\nsd = sqrt(n * p * (1 - p))   # 표준편차\nprint(E, sd, sum(probs))     # 확률의 합은 항상 1"
  },
  {
    "objectID": "pages/03_dist/binomial/index.html#스스로-확인",
    "href": "pages/03_dist/binomial/index.html#스스로-확인",
    "title": "이항분포",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — p = 0.5일 때와 p = 0.1일 때 분포의 모양은 어떻게 다른가?\n\n\n\n\n\n\\(p = 0.5\\)이면 평균 \\(np\\)를 중심으로 좌우 대칭이고, \\(p = 0.1\\)이면 왼쪽으로 치우친 비대칭이 됩니다. 다만 \\(n\\)을 충분히 키우면 어느 쪽이든 다시 종 모양에 가까워집니다.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — 정규근사가 잘 맞으려면 n과 p가 어떤 조건이면 좋을까?\n\n\n\n\n\n경험적으로 \\(np \\ge 5\\), \\(n(1-p) \\ge 5\\) 정도면 근사가 쓸 만합니다. \\(p\\)가 0이나 1에 가까울수록 더 큰 \\(n\\)이 필요합니다 — 앱에서 \\(p = 0.05\\)로 두고 \\(n\\)을 움직이며 확인해 보세요.\n\n\n\n\n← 이전: 확률변수와 기댓값 다음: 연속확률변수와 확률분포 →"
  },
  {
    "objectID": "pages/03_dist/normal/index.html",
    "href": "pages/03_dist/normal/index.html",
    "title": "정규분포와 표준화",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/03_dist/normal/index.html#개념",
    "href": "pages/03_dist/normal/index.html#개념",
    "title": "정규분포와 표준화",
    "section": "개념",
    "text": "개념\n키, 시험 점수처럼 평균 근처에 자료가 몰리고 양쪽으로 대칭인 분포를 정규분포 \\(N(m,\\,\\sigma^2)\\)로 나타냅니다. 곡선의 위치는 평균 \\(m\\)이, 퍼진 정도는 표준편차 \\(\\sigma\\)가 정합니다. 곡선 아래 구간의 넓이가 곧 확률입니다.\n\\[Z = \\frac{X - m}{\\sigma} \\;\\sim\\; N(0,\\,1)\\]\n\n\n수능 표준점수가 바로 표준화입니다. 과목마다 \\(m\\)과 \\(\\sigma\\)가 달라도 \\(Z\\)로 바꾸면 같은 자로 잴 수 있습니다.\n어떤 정규분포든 표준화하면 표준정규분포 \\(N(0, 1)\\)이 되므로, 확률 계산은 표준정규분포표 하나로 충분합니다. 아래 앱에서 \\(m\\), \\(\\sigma\\)를 움직여 곡선의 변화를 보고, 구간의 넓이가 표준화된 \\(Z\\) 구간의 넓이와 같음을 확인해 보세요."
  },
  {
    "objectID": "pages/03_dist/normal/index.html#만지며-배우기",
    "href": "pages/03_dist/normal/index.html#만지며-배우기",
    "title": "정규분포와 표준화",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 660\n\nfrom math import erf, sqrt\nfrom shiny import App, render, ui\nimport numpy as np\nimport matplotlib\nimport matplotlib.pyplot as plt\n\nmatplotlib.rcParams[\"font.size\"] = 10\n\nACCENT = \"#2563eb\"\nINK = \"#111827\"\nMUTED = \"#6b7280\"\n\ndef tufte(ax):\n    for side in (\"top\", \"right\"):\n        ax.spines[side].set_visible(False)\n    for side in (\"left\", \"bottom\"):\n        ax.spines[side].set_color(\"#9ca3af\")\n    ax.tick_params(colors=MUTED, labelsize=9)\n\ndef pdf(x, m, s):\n    return np.exp(-((x - m) ** 2) / (2 * s * s)) / (s * sqrt(2 * np.pi))\n\ndef cdf(x, m, s):\n    return 0.5 * (1 + erf((x - m) / (s * sqrt(2))))\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_slider(\"m\", \"평균 m\", min=40, max=100, value=70, step=1),\n        ui.input_slider(\"s\", \"표준편차 σ\", min=2, max=20, value=10, step=1),\n        ui.input_slider(\"ab\", \"구간 [a, b]\", min=20, max=120, value=(65, 75), step=1),\n        ui.input_checkbox(\"show_z\", \"표준정규분포 Z 겹쳐 보기\", False),\n        width=240,\n    ),\n    ui.card(\n        ui.card_header(\"정규분포 곡선 — 색칠한 넓이가 곧 확률\"),\n        ui.output_plot(\"curve\", height=\"360px\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @render.plot(alt=\"정규분포 곡선과 구간 확률\")\n    def curve():\n        m, s = input.m(), input.s()\n        a, b = input.ab()\n\n        if input.show_z():\n            fig, (ax1, ax2) = plt.subplots(1, 2)\n        else:\n            fig, ax1 = plt.subplots()\n\n        # 원래 분포 N(m, σ²)\n        xs = np.linspace(m - 4 * s, m + 4 * s, 400)\n        ax1.plot(xs, pdf(xs, m, s), color=INK, linewidth=1.5)\n        seg = np.linspace(a, b, 200)\n        ax1.fill_between(seg, pdf(seg, m, s), color=ACCENT, alpha=0.18)\n        ax1.axvline(m, color=\"#9ca3af\", linewidth=0.7, linestyle=\":\")\n        ax1.set_title(f\"N({m}, {s}²)\", fontsize=10, color=INK)\n        ax1.set_xlabel(\"x\", color=MUTED)\n        tufte(ax1)\n\n        # 표준화한 분포 N(0, 1)\n        if input.show_z():\n            za, zb = (a - m) / s, (b - m) / s\n            zs = np.linspace(-4, 4, 400)\n            ax2.plot(zs, pdf(zs, 0, 1), color=INK, linewidth=1.5)\n            zseg = np.linspace(max(za, -4), min(zb, 4), 200)\n            ax2.fill_between(zseg, pdf(zseg, 0, 1), color=ACCENT, alpha=0.18)\n            ax2.axvline(0, color=\"#9ca3af\", linewidth=0.7, linestyle=\":\")\n            ax2.set_title(\"N(0, 1)\", fontsize=10, color=INK)\n            ax2.set_xlabel(\"z\", color=MUTED)\n            tufte(ax2)\n\n        return fig\n\n    @render.ui\n    def readout():\n        m, s = input.m(), input.s()\n        a, b = input.ab()\n        p = cdf(b, m, s) - cdf(a, m, s)\n        za, zb = (a - m) / s, (b - m) / s\n        return ui.HTML(\n            f'&lt;p class=\"hs-readline\"&gt;'\n            f\"P({a} ≤ X ≤ {b}) = {p:.4f} — 표준화하면 \"\n            f\"P({za:.2f} ≤ Z ≤ {zb:.2f})와 같습니다. \"\n            f\"m과 σ를 바꿔도 z가 같으면 확률(넓이)은 변하지 않습니다.&lt;/p&gt;\"\n        )\n\napp = App(app_ui, server)\n\n\n정규분포 확률은 표준화와 표준정규분포의 누적확률로 계산합니다.\nfrom math import erf, sqrt\n\ndef cdf(z):                       # 표준정규분포 P(Z ≤ z)\n    return 0.5 * (1 + erf(z / sqrt(2)))\n\nm, s = 70, 10                     # X ~ N(70, 10²)\na, b = 65, 75\n\nza, zb = (a - m) / s, (b - m) / s # 표준화\nprint(cdf(zb) - cdf(za))          # P(65 ≤ X ≤ 75) = P(-0.5 ≤ Z ≤ 0.5)"
  },
  {
    "objectID": "pages/03_dist/normal/index.html#스스로-확인",
    "href": "pages/03_dist/normal/index.html#스스로-확인",
    "title": "정규분포와 표준화",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — σ를 두 배로 하면 곡선은 어떻게 변하는가?\n\n\n\n\n\n곡선이 옆으로 두 배 퍼지면서 봉우리는 절반 높이로 낮아집니다. 전체 넓이가 항상 1이어야 하기 때문입니다. 같은 구간 \\([a, b]\\)의 확률(넓이)이 어떻게 변하는지도 함께 관찰해 보세요.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — m = 70, σ = 10일 때 P(60 ≤ X ≤ 80)은? 표준화하면 어떤 Z 구간인가?\n\n\n\n\n\n\\(Z = (X - 70)/10\\)이므로 \\(P(-1 \\le Z \\le 1) \\approx 0.6827\\) — “평균 ± 1σ 안에 약 68%”라는 정규분포의 기본 성질입니다. 앱에서 구간을 \\([60, 80]\\)으로 맞춰 확인해 보세요.\n\n\n\n\n← 이전: 연속확률변수와 확률분포 다음: 표본추출 →"
  },
  {
    "objectID": "pages/04_infer/ci_mean/index.html",
    "href": "pages/04_infer/ci_mean/index.html",
    "title": "모평균의 신뢰구간",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/04_infer/ci_mean/index.html#개념",
    "href": "pages/04_infer/ci_mean/index.html#개념",
    "title": "모평균의 신뢰구간",
    "section": "개념",
    "text": "개념\n모표준편차 \\(\\sigma\\)를 아는 정규 모집단에서 크기 \\(n\\)인 표본을 뽑았을 때, 모평균 \\(m\\)의 신뢰구간은 표본평균 \\(\\bar{x}\\) 주위로 다음과 같이 만듭니다.\n\\[\\bar{x} - z\\frac{\\sigma}{\\sqrt{n}} \\;\\le\\; m \\;\\le\\; \\bar{x} + z\\frac{\\sigma}{\\sqrt{n}}\n\\qquad (z = 1.645,\\ 1.96,\\ 2.576)\\]\n\n\n구간의 폭은 \\(2z\\dfrac{\\sigma}{\\sqrt{n}}\\)입니다. \\(n\\)을 4배로 하면 \\(\\sqrt{n}\\)이 2배가 되어 폭이 절반으로 줄어듭니다.\n신뢰수준 95%의 뜻에 주의해야 합니다. “이 구간에 \\(m\\)이 있을 확률이 95%”가 아니라, 같은 방법으로 조사를 반복하면 만들어지는 구간들 중 약 95%가 \\(m\\)을 포함한다는 뜻입니다. 아래 앱은 (우리만 모르는) 진짜 모평균을 두고 100번 조사를 반복해 이 뜻을 눈으로 보여 줍니다. \\(m\\)을 놓친 구간이 평균적으로 몇 개쯤 나오는지 세어 보세요."
  },
  {
    "objectID": "pages/04_infer/ci_mean/index.html#만지며-배우기",
    "href": "pages/04_infer/ci_mean/index.html#만지며-배우기",
    "title": "모평균의 신뢰구간",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 640\n\nfrom math import sqrt\nfrom shiny import App, reactive, render, ui\nimport numpy as np\nimport matplotlib\nimport matplotlib.pyplot as plt\n\nmatplotlib.rcParams[\"font.size\"] = 10\n\nACCENT = \"#2563eb\"\nINK = \"#111827\"\nMUTED = \"#6b7280\"\n\ndef tufte(ax):\n    for side in (\"top\", \"right\"):\n        ax.spines[side].set_visible(False)\n    for side in (\"left\", \"bottom\"):\n        ax.spines[side].set_color(\"#9ca3af\")\n    ax.tick_params(colors=MUTED, labelsize=9)\n\nM, SIGMA = 70, 10          # 진짜 모평균(조사자는 모른다고 가정)\nZ = {\"90\": 1.645, \"95\": 1.96, \"99\": 2.576}\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_slider(\"n\", \"표본 크기 n\", min=10, max=100, value=25, step=10),\n        ui.input_select(\"level\", \"신뢰수준\", {\"90\": \"90%\", \"95\": \"95%\", \"99\": \"99%\"}),\n        ui.input_action_button(\"redraw\", \"새로 100번 조사\"),\n        width=240,\n    ),\n    ui.card(\n        ui.card_header(\"100개의 신뢰구간 — 모평균을 놓친 구간은 파랑\"),\n        ui.output_plot(\"ci_plot\", height=\"340px\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @reactive.calc\n    def intervals():\n        input.redraw()  # 버튼을 누르면 새로 100번 조사\n        n = input.n()\n        z = Z[input.level()]\n        xbar = np.random.normal(M, SIGMA, (100, n)).mean(axis=1)\n        half = z * SIGMA / sqrt(n)\n        return xbar - half, xbar + half\n\n    @render.plot(alt=\"100개의 신뢰구간과 모평균\")\n    def ci_plot():\n        lo, hi = intervals()\n        ys = np.arange(100)\n        hit = (lo &lt;= M) & (M &lt;= hi)\n\n        fig, ax = plt.subplots()\n        ax.hlines(ys[hit], lo[hit], hi[hit], color=\"#9ca3af\", linewidth=1)\n        ax.hlines(ys[~hit], lo[~hit], hi[~hit], color=ACCENT, linewidth=2.2)\n        ax.axvline(M, color=INK, linewidth=1, linestyle=\"--\")\n        ax.set_xlabel(\"x\", color=MUTED)\n        ax.set_yticks([])\n        tufte(ax)\n        return fig\n\n    @render.ui\n    def readout():\n        lo, hi = intervals()\n        hits = int(((lo &lt;= M) & (M &lt;= hi)).sum())\n        return ui.HTML(\n            f'&lt;p class=\"hs-readline\"&gt;'\n            f\"이번 100번의 조사 중 &lt;b&gt;{hits}개&lt;/b&gt; 구간이 모평균 m을 포함했습니다. \"\n            f\"신뢰수준 {input.level()}%의 뜻: 같은 방법으로 반복하면 약 \"\n            f\"{input.level()}%의 구간이 모평균을 포함한다 — 특정 구간 하나가 \"\n            f\"맞을 확률이 {input.level()}%라는 뜻이 아닙니다. \"\n            f\"신뢰수준을 높이거나 n을 줄이면 구간은 넓어집니다.&lt;/p&gt;\"\n        )\n\napp = App(app_ui, server)\n\n\n신뢰구간 계산은 공식 그대로 몇 줄이면 충분합니다.\nimport numpy as np\n\nsigma, n, z = 10, 25, 1.96          # σ를 아는 경우, 95% → z = 1.96\n\nsample = np.random.normal(70, sigma, n)   # 조사 한 번\nxbar = sample.mean()\n\nhalf = z * sigma / np.sqrt(n)             # 오차의 한계\nprint(xbar - half, xbar + half)           # 95% 신뢰구간 [x̄−1.96σ/√n, x̄+1.96σ/√n]"
  },
  {
    "objectID": "pages/04_infer/ci_mean/index.html#스스로-확인",
    "href": "pages/04_infer/ci_mean/index.html#스스로-확인",
    "title": "모평균의 신뢰구간",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — 신뢰수준을 90% → 99%로 바꾸면 그림에서 두 가지가 동시에 변한다. 무엇인가?\n\n\n\n\n\n구간이 넓어지고(\\(z\\)가 \\(1.645 \\to 2.576\\)), 모평균을 놓치는 파란 구간이 줄어듭니다(평균 10개 → 1개). 더 확실하게 잡으려면 더 넓게 던져야 한다는 맞바꿈 관계입니다.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — n = 25에서 n = 100으로 바꾸면 구간의 폭은 얼마나 줄어드는가?\n\n\n\n\n\n폭은 \\(2z\\sigma/\\sqrt{n}\\)이므로 \\(n\\)이 4배가 되면 폭은 절반이 됩니다. 95% 기준으로 \\(2 \\times 1.96 \\times 10/\\sqrt{25} = 7.84\\)에서 \\(2 \\times 1.96 \\times 10/\\sqrt{100} = 3.92\\)로 줄어듭니다. 정밀도를 2배 높이려면 조사 인원은 4배 필요합니다.\n\n\n\n\n← 이전: 표본평균의 분포 다음: 모비율의 추정 →"
  },
  {
    "objectID": "pages/04_infer/sample_mean/index.html",
    "href": "pages/04_infer/sample_mean/index.html",
    "title": "표본평균의 분포",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/04_infer/sample_mean/index.html#개념",
    "href": "pages/04_infer/sample_mean/index.html#개념",
    "title": "표본평균의 분포",
    "section": "개념",
    "text": "개념\n모평균 \\(m\\), 모표준편차 \\(\\sigma\\)인 모집단에서 크기 \\(n\\)인 표본을 뽑을 때, 표본평균 \\(\\bar{X}\\)는 표본마다 값이 달라지는 확률변수이고 그 자체로 분포를 가집니다.\n\\[E(\\bar{X}) = m, \\qquad \\sigma(\\bar{X}) = \\frac{\\sigma}{\\sqrt{n}}\\]\n\n\n02단원에서 시행을 반복할수록 상대도수가 확률에 가까워지는 것을 보았습니다. 같은 원리로, \\(n\\)이 커질수록 표본평균은 모평균 \\(m\\) 주위로 점점 촘촘히 몰립니다.\n놀라운 점은 모집단이 어떤 모양이든 — 균등하든, 한쪽으로 치우쳤든 — \\(n\\)이 충분히 크면 \\(\\bar{X}\\)의 분포가 정규분포 \\(N\\!\\left(m,\\ \\left(\\dfrac{\\sigma}{\\sqrt{n}}\\right)^{2}\\right)\\)에 가까워진다는 것입니다. 이것이 중심극한정리입니다. 아래 앱에서 모집단의 모양을 바꿔 가며 오른쪽 표본평균의 분포가 종 모양 곡선에 달라붙는 모습을 확인해 보세요."
  },
  {
    "objectID": "pages/04_infer/sample_mean/index.html#만지며-배우기",
    "href": "pages/04_infer/sample_mean/index.html#만지며-배우기",
    "title": "표본평균의 분포",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 660\n\nfrom math import sqrt\nfrom shiny import App, reactive, render, ui\nimport numpy as np\nimport matplotlib\nimport matplotlib.pyplot as plt\n\nmatplotlib.rcParams[\"font.size\"] = 10\n\nACCENT = \"#2563eb\"\nINK = \"#111827\"\nMUTED = \"#6b7280\"\n\ndef tufte(ax):\n    for side in (\"top\", \"right\"):\n        ax.spines[side].set_visible(False)\n    for side in (\"left\", \"bottom\"):\n        ax.spines[side].set_color(\"#9ca3af\")\n    ax.tick_params(colors=MUTED, labelsize=9)\n\nM, SIGMA = 50, 10  # 모든 모집단을 m=50, σ=10으로 맞춤\n\ndef draw_pop(dist, size):\n    if dist == \"unif\":\n        w = 10 * sqrt(3)\n        return np.random.uniform(M - w, M + w, size)\n    if dist == \"skew\":\n        return 40 + np.random.exponential(10, size)\n    return np.random.normal(M, SIGMA, size)\n\ndef normal_pdf(x, m, s):\n    return np.exp(-((x - m) ** 2) / (2 * s * s)) / (s * sqrt(2 * np.pi))\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_select(\n            \"dist\", \"모집단 분포\",\n            {\"unif\": \"균등분포\", \"skew\": \"치우친 분포(지수)\", \"norm\": \"정규분포\"},\n        ),\n        ui.input_slider(\"n\", \"표본 크기 n\", min=1, max=100, value=30),\n        ui.input_slider(\"reps\", \"표본의 개수\", min=100, max=2000, value=500, step=100),\n        ui.input_action_button(\"redraw\", \"다시 추출\"),\n        width=240,\n    ),\n    ui.card(\n        ui.card_header(\"모집단(왼쪽) vs 표본평균들의 분포(오른쪽)\"),\n        ui.output_plot(\"clt_plot\", height=\"340px\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @reactive.calc\n    def means():\n        input.redraw()  # 버튼을 누르면 새로 추출\n        samples = draw_pop(input.dist(), (input.reps(), input.n()))\n        return samples.mean(axis=1)\n\n    @render.plot(alt=\"모집단 분포와 표본평균의 분포\")\n    def clt_plot():\n        n = input.n()\n        pop = draw_pop(input.dist(), 20000)\n        xbar = means()\n\n        fig, (ax1, ax2) = plt.subplots(1, 2)\n\n        ax1.hist(pop, bins=40, density=True, color=\"#d1d5db\")\n        ax1.axvline(M, color=\"#9ca3af\", linewidth=0.7, linestyle=\":\")\n        ax1.set_title(\"population\", fontsize=10, color=INK)\n        ax1.set_xlabel(\"x\", color=MUTED)\n        tufte(ax1)\n\n        se = SIGMA / sqrt(n)\n        ax2.hist(xbar, bins=30, density=True, color=ACCENT, alpha=0.85)\n        xs = np.linspace(M - 4 * se, M + 4 * se, 300)\n        ax2.plot(xs, normal_pdf(xs, M, se), color=INK, linewidth=1.5)\n        ax2.set_title(\"sample means\", fontsize=10, color=INK)\n        ax2.set_xlabel(\"x-bar\", color=MUTED)\n        tufte(ax2)\n\n        return fig\n\n    @render.ui\n    def readout():\n        n = input.n()\n        se = SIGMA / sqrt(n)\n        obs = means().std()\n        return ui.HTML(\n            f'&lt;p class=\"hs-readline\"&gt;'\n            f\"이론값 σ/√n = 10/√{n} = {se:.2f} · \"\n            f\"관측된 표본평균들의 표준편차 = {obs:.2f}. \"\n            f\"모집단이 어떤 모양이든 n이 크면 표본평균의 분포는 \"\n            f\"정규분포 N(50, (10/√{n})²)에 가깝습니다 — 중심극한정리.&lt;/p&gt;\"\n        )\n\napp = App(app_ui, server)\n\n\n표본을 여러 번 뽑아 표본평균들의 표준편차를 이론값과 비교합니다.\nimport numpy as np\n\nn = 30                                        # 표본 크기\nsamples = 40 + np.random.exponential(10, (500, n))  # 치우친 모집단, 500개 표본\nxbar = samples.mean(axis=1)                   # 표본평균 500개\n\nprint(xbar.mean())          # ≈ 50  (= 모평균 m)\nprint(xbar.std())           # ≈ 10 / np.sqrt(n) = 1.83 (= σ/√n)"
  },
  {
    "objectID": "pages/04_infer/sample_mean/index.html#스스로-확인",
    "href": "pages/04_infer/sample_mean/index.html#스스로-확인",
    "title": "표본평균의 분포",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — n을 1에서 100으로 키우면 오른쪽 그림은 어떻게 변하는가?\n\n\n\n\n\n표본평균들의 분포가 \\(m = 50\\) 주위로 점점 좁게 몰립니다. 표준편차가 \\(\\sigma/\\sqrt{n}\\)이므로 \\(n = 100\\)이면 \\(10/\\sqrt{100} = 1\\) — 모집단 표준편차의 \\(\\frac{1}{10}\\)입니다. 또한 \\(n = 1\\)일 때는 모집단 모양 그대로이지만, \\(n\\)이 커지면 치우친 분포에서도 종 모양이 나타납니다.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — ’치우친 분포(지수)’에서 n = 2일 때와 n = 50일 때, 정규 곡선과의 들어맞음을 비교해 보라.\n\n\n\n\n\n\\(n = 2\\)에서는 표본평균의 분포도 오른쪽으로 치우쳐 곡선과 어긋나지만, \\(n = 50\\)이면 거의 완벽하게 겹칩니다. 중심극한정리가 말하는 “충분히 큰 \\(n\\)”의 효과입니다. 정규 모집단은 \\(n\\)이 작아도 처음부터 정확히 정규분포라는 점도 확인해 보세요.\n\n\n\n\n← 이전: 표본추출 다음: 모평균의 신뢰구간 →"
  },
  {
    "objectID": "pages/05_society/explorer/index.html",
    "href": "pages/05_society/explorer/index.html",
    "title": "데이터 탐색기",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/05_society/explorer/index.html#개념",
    "href": "pages/05_society/explorer/index.html#개념",
    "title": "데이터 탐색기",
    "section": "개념",
    "text": "개념\n자료를 손에 넣었을 때 가장 먼저 할 일은 탐색입니다. 탐색의 기본은 자료를 세 가지 방식으로 보는 것입니다. 표로 원자료를 하나하나 확인하고, 그래프로 분포의 전체 모양을 살피고, 평균·중앙값·표준편차 같은 요약값으로 특징을 숫자 몇 개로 압축합니다.\n\n\n아래 데이터는 교육용으로 만든 가상 데이터입니다. 실제 탐구에서는 우리 반 친구들에게 직접 설문해서 나만의 데이터를 모아 보세요.\n세 방식은 서로를 보완합니다. 표만 보면 전체 모양이 안 보이고, 요약값만 보면 특이한 값이 숨습니다. 아래 앱에서 가상의 고등학생 25명 데이터를 변수별로 골라 표·그래프·요약값을 한 번에 살펴보세요."
  },
  {
    "objectID": "pages/05_society/explorer/index.html#만지며-배우기",
    "href": "pages/05_society/explorer/index.html#만지며-배우기",
    "title": "데이터 탐색기",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 760\n\nfrom shiny import App, render, ui\nimport numpy as np\nimport matplotlib\nimport matplotlib.pyplot as plt\n\nmatplotlib.rcParams[\"font.size\"] = 10\n\nACCENT = \"#2563eb\"\nINK = \"#111827\"\nMUTED = \"#6b7280\"\n\n# 교육용 예시(가상) 데이터 — 고등학생 25명\nDATA = {\n    \"키(cm)\": [172.5, 158.3, 165.0, 181.2, 160.4, 175.8, 168.9, 155.2,\n               178.4, 162.7, 170.1, 152.8, 183.6, 166.3, 159.5, 174.2,\n               161.8, 169.4, 187.0, 157.6, 171.3, 164.5, 176.9, 153.9, 167.2],\n    \"통학시간(분)\": [15, 30, 10, 45, 25, 5, 60, 20, 35, 12, 40, 8, 55,\n                    18, 22, 70, 28, 14, 33, 48, 9, 26, 38, 16, 52],\n    \"수면시간(시간)\": [6.5, 7.2, 5.8, 6.0, 7.5, 8.0, 5.5, 6.8, 7.0, 6.2,\n                      8.5, 7.8, 5.2, 6.4, 7.1, 6.6, 8.2, 5.9, 6.1, 7.4,\n                      6.9, 5.6, 7.7, 6.3, 8.8],\n    \"성별\": [\"남\", \"여\", \"여\", \"남\", \"여\", \"남\", \"남\", \"여\", \"남\", \"여\",\n             \"남\", \"여\", \"남\", \"여\", \"여\", \"남\", \"여\", \"남\", \"남\", \"여\",\n             \"남\", \"여\", \"남\", \"여\", \"남\"],\n}\n\n# 그림 축 라벨은 영어만 사용 (한글 변수명 → 영어 라벨)\nAXIS = {\n    \"키(cm)\": \"height (cm)\",\n    \"통학시간(분)\": \"commute (min)\",\n    \"수면시간(시간)\": \"sleep (h)\",\n    \"성별\": \"gender\",\n}\n\ndef tufte(ax):\n    for side in (\"top\", \"right\"):\n        ax.spines[side].set_visible(False)\n    for side in (\"left\", \"bottom\"):\n        ax.spines[side].set_color(\"#9ca3af\")\n    ax.tick_params(colors=MUTED, labelsize=9)\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_select(\"var\", \"변수\", {k: k for k in DATA}),\n        width=240,\n    ),\n    ui.card(\n        ui.card_header(\"데이터 표\"),\n        ui.output_ui(\"table\"),\n    ),\n    ui.card(\n        ui.card_header(\"분포\"),\n        ui.output_plot(\"dist\", height=\"280px\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @render.ui\n    def table():\n        keys = list(DATA.keys())\n        th = 'style=\"padding:2px 6px;border-bottom:1px solid #9ca3af;text-align:center;\"'\n        td = 'style=\"padding:2px 6px;border-bottom:1px solid #e5e7eb;text-align:center;\"'\n        head = \"\".join(f\"&lt;th {th}&gt;{k}&lt;/th&gt;\" for k in [\"번호\"] + keys)\n        rows = []\n        for i in range(25):\n            cells = f\"&lt;td {td}&gt;{i + 1}&lt;/td&gt;\" + \"\".join(\n                f\"&lt;td {td}&gt;{DATA[k][i]}&lt;/td&gt;\" for k in keys\n            )\n            rows.append(f\"&lt;tr&gt;{cells}&lt;/tr&gt;\")\n        return ui.HTML(\n            '&lt;div style=\"max-height:220px;overflow-y:auto;\"&gt;'\n            '&lt;table style=\"width:100%;border-collapse:collapse;font-size:0.8rem;\"&gt;'\n            f\"&lt;thead&gt;&lt;tr&gt;{head}&lt;/tr&gt;&lt;/thead&gt;\"\n            f\"&lt;tbody&gt;{''.join(rows)}&lt;/tbody&gt;&lt;/table&gt;&lt;/div&gt;\"\n        )\n\n    @render.plot(alt=\"선택한 변수의 분포\")\n    def dist():\n        v = input.var()\n        fig, ax = plt.subplots()\n        if v == \"성별\":\n            counts = [DATA[\"성별\"].count(\"남\"), DATA[\"성별\"].count(\"여\")]\n            ax.bar([\"M\", \"F\"], counts, color=\"#9ca3af\", width=0.5)\n            ax.set_xlabel(\"gender\", color=MUTED)\n            ax.set_ylabel(\"count\", color=MUTED)\n        else:\n            vals = np.array(DATA[v])\n            ax.hist(vals, bins=8, color=ACCENT, alpha=0.85, edgecolor=\"white\")\n            ax.axvline(vals.mean(), color=ACCENT, linewidth=1.6, label=\"mean\")\n            ax.axvline(np.median(vals), color=INK, linewidth=1.2,\n                       linestyle=\"--\", label=\"median\")\n            ax.set_xlabel(AXIS[v], color=MUTED)\n            ax.set_ylabel(\"count\", color=MUTED)\n            ax.legend(frameon=False, fontsize=9)\n        tufte(ax)\n        return fig\n\n    @render.ui\n    def readout():\n        v = input.var()\n        if v == \"성별\":\n            nm = DATA[\"성별\"].count(\"남\")\n            nf = DATA[\"성별\"].count(\"여\")\n            n = nm + nf\n            line = (f\"남학생 {nm}명({nm / n * 100:.0f}%), \"\n                    f\"여학생 {nf}명({nf / n * 100:.0f}%)으로 모두 {n}명입니다. \"\n                    \"범주형 자료는 평균 대신 도수와 비율로 요약합니다.\")\n        else:\n            vals = np.array(DATA[v])\n            line = (f\"{v}의 평균은 {vals.mean():.1f}, 중앙값은 {np.median(vals):.1f}, \"\n                    f\"표준편차는 {vals.std():.1f}, 최솟값은 {vals.min():.1f}, \"\n                    f\"최댓값은 {vals.max():.1f}입니다.\")\n        return ui.HTML(f'&lt;p class=\"hs-readline\"&gt;{line}&lt;/p&gt;')\n\napp = App(app_ui, server)\n\n\n요약값 계산은 numpy 몇 줄이면 충분합니다.\nimport numpy as np\n\ncommute = [15, 30, 10, 45, 25, 5, 60, 20, 35, 12, 40, 8, 55,\n           18, 22, 70, 28, 14, 33, 48, 9, 26, 38, 16, 52]\n\nvals = np.array(commute)\nprint(vals.mean())        # 평균\nprint(np.median(vals))    # 중앙값\nprint(vals.std())         # 표준편차\nprint(vals.min(), vals.max())  # 최솟값, 최댓값"
  },
  {
    "objectID": "pages/05_society/explorer/index.html#스스로-확인",
    "href": "pages/05_society/explorer/index.html#스스로-확인",
    "title": "데이터 탐색기",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — 통학시간의 평균과 중앙값 중 어느 쪽이 더 큰가? 히스토그램 모양으로 이유를 설명해 보라.\n\n\n\n\n\n평균(약 29.4분)이 중앙값(26분)보다 큽니다. 히스토그램을 보면 통학시간이 긴 쪽으로 꼬리가 길게 늘어져 있는데, 60분·70분 같은 큰 값이 평균을 끌어올리기 때문입니다. 분포가 한쪽으로 치우치면 평균과 중앙값이 벌어집니다.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — 성별을 선택하면 왜 평균·표준편차가 나오지 않는가?\n\n\n\n\n\n성별은 수가 아니라 범주(남/여)인 범주형 변수이기 때문입니다. 범주형 자료는 각 범주의 도수와 비율로 요약하고, 그래프도 히스토그램이 아니라 막대그래프를 씁니다. 변수의 종류에 따라 요약 방법이 달라집니다.\n\n\n\n\n← 이전: 모비율의 추정 다음: 통계 그래프 만들기 →"
  },
  {
    "objectID": "pages/05_society/poll/index.html",
    "href": "pages/05_society/poll/index.html",
    "title": "여론조사 읽기",
    "section": "",
    "text": "← 빛스탯 HS 홈"
  },
  {
    "objectID": "pages/05_society/poll/index.html#개념",
    "href": "pages/05_society/poll/index.html#개념",
    "title": "여론조사 읽기",
    "section": "개념",
    "text": "개념\n선거철 뉴스에는 “지지율 45%, 오차범위 ±3.1%p(95% 신뢰수준)” 같은 문장이 자주 나옵니다. 오차범위는 표본으로 조사한 지지율이 실제 지지율과 얼마나 다를 수 있는지의 폭이고, 신뢰수준은 그 폭을 믿을 수 있는 정도입니다. 두 후보의 격차가 오차범위 안이라면 “누가 앞선다”고 단정할 수 없고 경합으로 읽어야 합니다.\n\n\n이 오차범위 계산의 근거가 바로 04단원 모비율의 추정입니다. 최대오차는 \\(z\\sqrt{0.25/n}\\)으로 구합니다.\n아래 앱은 가상의 선거 여론조사입니다. 응답자 수와 지지율, 신뢰수준을 바꿔 가며 오차범위가 어떻게 변하고, 같은 격차가 언제 “경합”이 되고 언제 “우세”가 되는지 확인해 보세요."
  },
  {
    "objectID": "pages/05_society/poll/index.html#만지며-배우기",
    "href": "pages/05_society/poll/index.html#만지며-배우기",
    "title": "여론조사 읽기",
    "section": "만지며 배우기",
    "text": "만지며 배우기\n\n인터랙티브 앱파이썬 코드\n\n\n#| '!! shinylive warning !!': |\n#|   shinylive does not work in self-contained HTML documents.\n#|   Please set `embed-resources: false` in your metadata.\n#| standalone: true\n#| viewerHeight: 640\n\nfrom math import sqrt\nfrom shiny import App, render, ui\nimport numpy as np\nimport matplotlib\nimport matplotlib.pyplot as plt\n\nmatplotlib.rcParams[\"font.size\"] = 10\n\nACCENT = \"#2563eb\"\nINK = \"#111827\"\nMUTED = \"#6b7280\"\n\nZ = {\"95\": 1.96, \"99\": 2.576}\n\ndef tufte(ax):\n    for side in (\"top\", \"right\"):\n        ax.spines[side].set_visible(False)\n    for side in (\"left\", \"bottom\"):\n        ax.spines[side].set_color(\"#9ca3af\")\n    ax.tick_params(colors=MUTED, labelsize=9)\n\napp_ui = ui.page_sidebar(\n    ui.sidebar(\n        ui.input_slider(\"n\", \"응답자 수 n\", min=300, max=3000,\n                        value=1000, step=100),\n        ui.input_slider(\"a\", \"후보 A 지지율(%)\", min=30, max=70,\n                        value=45, step=1),\n        ui.input_select(\"level\", \"신뢰수준\", {\"95\": \"95%\", \"99\": \"99%\"}),\n        width=240,\n    ),\n    ui.card(\n        ui.card_header(\"가상 여론조사 — 막대 위 선이 오차범위\"),\n        ui.output_plot(\"bars\", height=\"320px\"),\n    ),\n    ui.card(ui.output_ui(\"readout\")),\n    fillable=True,\n)\n\ndef server(input, output, session):\n    @render.plot(alt=\"두 후보 지지율과 오차범위\")\n    def bars():\n        a = input.a()\n        b = 90 - a          # 무응답 10% 고정\n        moe = Z[input.level()] * sqrt(0.25 / input.n()) * 100\n\n        fig, ax = plt.subplots()\n        ax.bar([\"A\", \"B\"], [a, b], color=[ACCENT, \"#9ca3af\"], width=0.5,\n               yerr=moe, capsize=8,\n               error_kw={\"ecolor\": INK, \"linewidth\": 1.2})\n        ax.set_ylabel(\"support (%)\", color=MUTED)\n        ax.set_ylim(0, 80)\n        tufte(ax)\n        return fig\n\n    @render.ui\n    def readout():\n        a = input.a()\n        b = 90 - a\n        moe = Z[input.level()] * sqrt(0.25 / input.n()) * 100\n        gap = abs(a - b)\n        leader = \"A\" if a &gt; b else \"B\"\n        if gap &lt;= 2 * moe:\n            verdict = (f\"두 후보 격차 {gap}%p가 오차범위(2배 기준 \"\n                       f\"{2 * moe:.1f}%p) 안이므로 '경합'으로 읽어야 합니다.\")\n        else:\n            verdict = (f\"두 후보 격차 {gap}%p가 오차범위(2배 기준 \"\n                       f\"{2 * moe:.1f}%p) 밖이므로 {leader}가 앞선다고 \"\n                       \"볼 수 있습니다.\")\n        line = (f\"오차범위 ±{moe:.1f}%p ({input.level()}% 신뢰수준) — {verdict} \"\n                \"기사를 읽을 때는 조사기관·응답률·조사기간도 꼭 확인하세요.\")\n        return ui.HTML(f'&lt;p class=\"hs-readline\"&gt;{line}&lt;/p&gt;')\n\napp = App(app_ui, server)\n\n\n오차범위(최대오차) 계산은 한 줄입니다.\nfrom math import sqrt\n\nn = 1000                    # 응답자 수\nz = 1.96                    # 95% 신뢰수준 (99%는 2.576)\n\nmoe = z * sqrt(0.25 / n) * 100   # 최대오차 ±%p\nprint(f\"±{moe:.1f}%p\")           # ±3.1%p\n\na, b = 45, 45               # 두 후보 지지율(%)\nprint(abs(a - b) &lt;= 2 * moe)     # True면 '경합'"
  },
  {
    "objectID": "pages/05_society/poll/index.html#스스로-확인",
    "href": "pages/05_society/poll/index.html#스스로-확인",
    "title": "여론조사 읽기",
    "section": "스스로 확인",
    "text": "스스로 확인\n\n\n\n\n\n\n힌트문제 1 — 응답자 수를 4배로 늘리면 오차범위는 어떻게 변하는가?\n\n\n\n\n\n오차범위는 \\(\\sqrt{n}\\)에 반비례하므로 절반이 됩니다. 앱에서 n을 500에서 2000으로 바꿔 확인해 보세요. 오차범위를 절반으로 줄이려면 조사 비용은 4배가 든다는 뜻이기도 합니다.\n\n\n\n\n\n\n\n\n\n힌트문제 2 — 격차가 오차범위 안인데 ’A 후보 역전!’이라고 보도하면 왜 문제인가?\n\n\n\n\n\n오차범위 안의 격차는 표본을 다시 뽑으면 뒤집힐 수도 있는, 통계적으로 구분할 수 없는 차이이기 때문입니다. 실제 지지율은 그대로인데 표본의 우연한 변동만으로 순위가 바뀌어 보일 수 있으므로, ’역전’이 아니라 ’경합’으로 읽는 것이 올바른 해석입니다.\n\n\n\n\n← 이전: 통계 그래프 만들기 다음: 통계적 탐구 프로젝트 →"
  }
]